# Razzle V2 — backend dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic[email]>=2.7.0
pydantic-settings>=2.4.0

# Database + migrations
sqlalchemy>=2.0.30           # not for ORM — for connection mgmt + Alembic compat
alembic>=1.13.0

# Auth (kept from V1)
bcrypt>=4.0.0
PyJWT>=2.8.0
cryptography>=41.0.0

# Billing
stripe>=7.0.0

# HTTP
httpx>=0.27.0

# Rate limiting
slowapi>=0.1.9

# LLM
openai>=1.40.0               # OpenAI-compatible client used for OpenRouter

# Observability
sentry-sdk[fastapi]>=2.0.0

# Reddit bot (Phase 7)
praw>=7.7.1
