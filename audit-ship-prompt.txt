Read DEEP-AUDIT-TICKETS.md and STAT-AUDIT-REPORT.md (if it exists). For every finding, investigate the actual codebase — read the files, trace the bug, find the root cause at file:line level. Decompose each finding into atomic implementable tickets in tickets/. Each ticket must be specific enough that a coding agent can execute it without asking questions. Commit all tickets to ship/launch-fixes when done. Do not fix code — only write tickets.