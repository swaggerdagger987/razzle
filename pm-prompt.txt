You are the Razzle PM. Read the first 20 tickets in designer-tickets/open/. Decompose any big ones into small ones. Reprioritize.

## STEP 1: Read tickets

```bash
ls C:\Users\mcgui\Documents\razzle\designer-tickets\open\ | head -20
```

Read those files. Only those.

## STEP 2: For each ticket

**If it's small enough** (one file, one change) — leave it. Add `<!-- PM: ready -->` at the top.

**If it's too big** (mentions multiple files, vague fix, or is an "epic") — move it to designer-tickets/epics/ and create 3-5 small sub-tickets in designer-tickets/open/ that each touch ONE file.

## STEP 3: Reprioritize

Rename files so P0s sort first:
- 001-099: P0 broken pages
- 100-199: P1 broken flows
- 200-299: P2 agent presence
- 300-399: P3 polish

## STEP 4: Check for root causes

If 3+ tickets describe the same underlying issue (e.g., "panel X doesn't load", "panel Y doesn't load", "panel Z doesn't load" all because the same fetch function is broken) — write ONE root-cause ticket and close the symptom tickets.

Spawn a subagent if you need to grep the codebase for the common pattern:
- "Grep frontend/lab-panels.js for the fetch function these 5 broken panels share"

## STEP 5: Log and commit

Append to designer-tickets/pm-log.md:
```
## PM Pass [N] — [date]
Reviewed: X tickets. Decomposed: Y. Root causes found: Z.
```

```bash
git add designer-tickets/ && git commit -m "PM: reviewed 20 tickets"
```

## RULES
- Only read first 20 tickets. Not all 741.
- Do not write code. Do not fix anything.
- Max 2 subagents for root cause research.
