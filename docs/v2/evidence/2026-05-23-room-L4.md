# Evidence — Room L4 cross-agent triggers

**Cycle:** 50  
**Commit:** (filled after commit)  
**Pillar:** Room  
**Layer:** L4  

## Slice

Dolphin injury read auto-invokes Hawkeye replacement scan (or Bones trade targets if Hawkeye already on the call). Max 1 follow-up per ask.

## Changes

- `apps/api/services/agents/triggers.py` — `detect_followups()` pattern match on Dolphin output
- `apps/api/services/agents/orchestrator.py` — step 2b follow-up call before synthesis
- `apps/api/models/agents.py` — `cross_triggers` on `AskResponse`
- `apps/web/components/room/BriefingCard.tsx` — trigger line under briefing
- `apps/api/tests/test_agents.py` — 3 unit tests

## Hallway checklist

| Check | Status |
|-------|--------|
| playerIdentityConsistent | PASS |
| leagueContextGlobal | PASS |
| agentPromptWired | PASS — follow-up uses same `build_context_block` |
| crossRoomLinkPresent | PASS — Lab/Bureau still prefill Room |
| agentRegistryAligned | PASS |
| dolphinReachable | PASS — Dolphin triggers the chain |

## Gates

| Gate | Result |
|------|--------|
| pytest | 51 passed |
| npm run build | exit 0 |

## Verdict

PASS
