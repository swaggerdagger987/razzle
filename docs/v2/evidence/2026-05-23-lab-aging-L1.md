# Evidence — Lab L1 aging curves (Cycle 15)

## Slice
- **Route:** `/lab/aging`
- **Pillar:** Lab L1 launch panel #8
- **Agent:** Octo (Quant)

## API
```bash
JWT_SECRET=test python -c "
from apps.api.services.panels.dispatcher import run_panel
r = run_panel('aging', {'position': 'WR'})
wr = r['positions']['WR']
print('peak_age', wr['peak_age'], 'curve_pts', len(wr['curve']), 'players', len(wr['players']))
"
# peak_age 23.8 curve_pts 60 players 188
```

## Hallway checklist
| Check | Status |
|-------|--------|
| playerIdentityConsistent | Player dot click + past-peak table → Player Sheet |
| leagueContextGlobal | Context bar on all routes (unchanged) |
| agentPromptWired | Room footer prefill: position peak + top player age/PPG |
| crossRoomLinkPresent | Player Sheet panels → `/lab/aging`; footer → `/room?agent=octo&q=...` |
| agentRegistryAligned | `octo.labPanels` includes `aging` (catalog slug) |
| dolphinReachable | N/A — quant panel, no injury column |

## Build
- `npm run build` — exit 0
- `pytest apps/api/tests -q` — 25 pass, 2 pre-existing snapshot fails

## Verdict
**PASS** — dedicated renderer, Pro gate, Reddit sell-window screenshot narrative
