# Explore L3 — Custom Formula Builder MVP

**Cycle:** 30  
**Commit:** (filled after commit)  
**Pillar:** Explore · **Layer:** L3

## Slice

Weighted composite formulas on `/explore`: localStorage persist (max 3), modal builder, dynamic table columns, client-side sort, Octo Room hallway link.

## Hallway checklist

| Check | Status |
|-------|--------|
| playerIdentityConsistent | PASS — row click → Player Sheet |
| leagueContextGlobal | PASS — context bar unchanged |
| agentPromptWired | N/A — compute is client-side |
| crossRoomLinkPresent | PASS — `toRoom` Octo after save |
| agentRegistryAligned | PASS — Octo quant owner on Room link |
| dolphinReachable | N/A |

## Verification

```bash
JWT_SECRET=test .venv-v2/bin/pytest apps/api/tests -q  # 35 passed, 2 snapshot pre-existing
npm run build                                          # exit 0
curl localhost:8000/api/health                         # ok
curl localhost:3000/explore                          # 200
```

## Manual

1. Open `/explore` → click **+ formula**
2. Name composite, add stat + weight → **Save Formula**
3. New column appears; table sorts by composite
4. **ask Octo in film room →** link prefills Room question

## Verdict

PASS
