# Evidence — Room L3 six-sprite pixel canvas

**Cycle:** 49  
**Commit:** (filled after commit)  
**Pillar:** Room  
**Layer:** L3  

## Slice

All six staff sprites on pixel canvas — no silent rebind. Canvas restored after board DELETE (cycle 43).

## Changes

- `packages/pixel-room/src/constants.ts` — `AgentId` widened to all six registry ids; sprite prefix + WORK_TILES per agent
- `packages/pixel-room/src/engine.ts` — six default agents on floor
- `packages/pixel-room/src/sprites.ts` — load six idle/walk sheets
- `apps/web/public/pixel-room/characters/` — 12 PNGs (legacy quarry → agent-id naming)
- `apps/web/components/room/SituationRoom.tsx` — PixelRoom restored; `toPixelAgent` is identity cast

## Hallway checklist

| Check | Status |
|-------|--------|
| playerIdentityConsistent | PASS — `?name=` in header subtitle; Player Sheet links unchanged |
| leagueContextGlobal | PASS — context bar on all routes; `agentContextPayload()` in asks |
| agentPromptWired | PASS — `/api/agents/ask` with league + player context |
| crossRoomLinkPresent | PASS — Bureau/Lab `toRoom()` prefill still routes here |
| agentRegistryAligned | PASS — six ids match `@razzle/agents` |
| dolphinReachable | PASS — Dolphin sprite + roster pick + injury routing |

## Gates

| Gate | Result |
|------|--------|
| pytest apps/api/tests -q | 48 passed |
| npm run build | exit 0; `/room` 6.99 kB |
| Gate 4 pixel canvas | PASS — six sprites, walk/work/select restored |

## Verdict

PASS
