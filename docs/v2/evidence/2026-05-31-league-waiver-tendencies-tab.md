# Evidence — league-waiver-tendencies-tab — 2026-05-31

## Commands

```bash
npm run build --workspace=apps/web  # exit 0
JWT_SECRET=test python3 -m pytest apps/api/tests -q --ignore=apps/api/tests/test_screener_snapshot.py  # 52 passed
```

## Trust

T2, T5 — waiver wire fingerprints visible in Bureau nav.
