# Evidence — League L4 trade-finder (Cycle 47)

**Commit:** (filled after Phase 5 commit)

## Slice
- **Pillar:** League
- **Layer:** L4
- **Feature:** `/league/[id]/trade-finder` — Bones value-matched trade suggestions

## API
```bash
curl -s -X POST http://localhost:8000/api/bureau/trade-finder \
  -H "Content-Type: application/json" \
  -d '{"league_id":"test","user_id":"test"}'
# → {"error":"league not found","league_id":"test"}  (200, not 500)
```

## Tests
```bash
JWT_SECRET=test-secret .venv-v2/bin/pytest apps/api/tests/test_bureau_trade_finder.py -q
# 4 passed
JWT_SECRET=test-secret .venv-v2/bin/pytest apps/api/tests -q
# 46 passed, 2 pre-existing snapshot failures
```

## Build
```bash
npm run build  # exit 0
```

## Hallway checklist
- [x] playerIdentityConsistent — Room ask includes give player
- [x] leagueContextGlobal — Bureau route, needsUser
- [x] agentPromptWired — Room question includes players + gap
- [x] crossRoomLinkPresent — pressure-map, trade-network, /lab/trade-values
- [x] agentRegistryAligned — Bones owns trade-finder in registry
- [x] dolphinReachable — N/A

## Verdict
PASS
