# Evidence — Lab L4 roster tools Pro teaser (cycle 89)

`panel-upgrade-teaser.ts`: `rosterbuilder` + `roster-value` rows and pitches.

`npm run build --workspace=apps/web` exit 0. `pytest apps/api/tests -q` 51 passed, 5 skipped.
