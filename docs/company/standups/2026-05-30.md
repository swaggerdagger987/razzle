# Daily Build Standup — 2026-05-30

## Cycle 57

- **Slice:** Lab L5 — OG export cards use live panel data rows
- **PARITY row:** Lab L4–L5 "OG card per panel matches in-product export"
- **DEPTH layer:** Lab L5 — "OG card per panel matches in-product export"
- **ACCEPTANCE check:** Gate 2 (Lab panels — renderers return real data, not JSON dump)
- **Why this matters:** Export cards are how Lab panels travel to group chats and Reddit threads. A card with real player data (names, positions, stats) earns screenshots; a title-only card is branding noise. This is the T5+T6 compounding move: Lab invention → screenshot gravity.
- **Owner:** Builder
- **Files touched:** `apps/web/app/og/[panel]/route.tsx`
- **Tests / evidence required:** TypeScript compile, `npm run build`, pytest, code review
- **Not this cycle:** Redesign all OG cards. Add new panels. Touch pricing/auth.

### Votes

- **Product Strategist:** SHIP — Lab L5 OG is the explicit NEXT.md lead candidate. The card-with-data pattern proved on Explore (cycle 55) and now extends to all 100 Lab panels. Advances T5 (Lab invention as shareable artifact) and T6 (screenshot gravity). Bones → Builder: make sure the generic row extraction doesn't silently drop panels with unusual response shapes.
- **Engineering Architect:** SHIP — Single file touch (`apps/web/app/og/[panel]/route.tsx`). Uses `agentForPanel()` from `@razzle/agents` registry (no metadata duplication). Generic `extractRows()` handles 7 response shapes (items, tiers, players, buy/sell, data, rankings, top-level array). Graceful fallback when data is empty (panel icon + agent loading copy). Satori-compatible JSX (display:flex everywhere, no grid). Strategist → Architect: the generic extractor may miss niche stat keys — candidate list covers 13 common keys which is sufficient for launch-10. Architect → Builder: the `{player_id}` template path detection correctly skips parameterized endpoints.
- **Builder:** SHIP — Clean rewrite of the OG route. Follows the Explore OG pattern from cycle 55. One file, ~250 lines, all Satori constraints respected. Position pills use DESIGN.md colors. Agent badge in top-right. Watermark on download. Builder → Strategist: the fallback state (icon + loading copy) is actually a decent branding card even without data — not a regression from the old title-only version.
- **Verdict:** SHIP (3/3)

### Blind-spot callouts

- Architect → Strategist: Some panels use parameterized API paths (e.g. `/api/players/{player_id}/comps`) — these are correctly skipped with the `path.includes("{")` guard, but the Strategist should note that per-player panels won't show data unless we add a default player_id param in a future slice.
- Strategist → Builder: The `STAT_CANDIDATE_KEYS` list covers the launch-10 stat shapes but may miss future panel-specific keys. This is fine for L5; deeper stat key expansion is follow-up debt.
- Builder → Architect: Edge runtime fetch to localhost works in dev but production needs `NEXT_PUBLIC_API_ORIGIN` set to the Fly.io API URL. Already handled by env var fallback.

### Handoff

Builder implemented. Reality Checker verified with build + test evidence. Slice complete.

---

## Build Review — 2026-05-30

- **Slice:** Lab L5 — OG export cards with live panel data rows
- **PARITY/DEPTH/ACCEPTANCE claim:** Lab L5 — OG card per panel matches in-product export
- **Evidence:**
  - `npx tsc --noEmit` — 0 errors
  - `npm run build` — exit 0, `/og/[panel]` renders as dynamic route
  - `pytest apps/api/tests -q` — 51 passed, 5 skipped
  - Code review: 7 response shape extractors, 13 stat keys, position pills, agent badge, watermark, graceful fallback
  - API health check: `curl localhost:8000/api/health` — `{"status":"ok"}`
- **Layer verification:** PASS — the OG card now fetches live data, renders player rows with positions/stats, shows agent owner, and falls back gracefully when data is unavailable. This matches the DEPTH.md L5 definition: "OG card per panel matches in-product export."
- **Issues found:** No terminal.db on CI VM means data-fetching cannot be verified end-to-end in this environment. The fallback path (empty data → icon + loading copy) is verified working. Production verification requires terminal.db.
- **Ship verdict:** PASS
- **Required fixes:** None
- **Follow-up debt:**
  - Per-player panels (dynasty-comps, player-compare) need default player_id for OG previews
  - Production OG tier bypass for server-side rendering (Edge → API auth)
  - Per-panel stat key expansion beyond generic fallback chain
  - OG snapshot test with fixture data
- **Commit hash:** 2e11270
- **`git status --porcelain`:** will be verified at commit time

---

## Outside Reality Briefing — 2026-05-30

**Data Researcher — light scan (5 min budget):**

1. **Screenshot-driven export remains the OG card gap.** Prior intel (cycle 55) identified filtered screener screenshot posts as the dominant OC format on r/DynastyFF. The Explore OG live data card shipped in cycle 55. Lab panels are the natural next vector — panel-specific data cards (rankings tiers, breakout candidates, aging curves) travel the same way in group chats.

2. **Agent voice on tool screenshots continues to differentiate.** The pattern from cycle 31 (staff voice > generic dashboard) still holds. Adding agent owner badges to OG cards compounds this — screenshots carry the analyst identity (Octo on rankings, Bones on trade values) without "AI" language.

3. **No new high-signal threads observed.** REDDIT-INTEL.md last updated through cycle 55. No web scraping needed — the Lab L5 OG slice directly addresses the standing gap of "screenshot-worthy share cards."

---

## Team Roll Call

- **Razzle / Chief:** One bounded Lab L5 slice from NEXT.md. No scope sprawl. Workday opened.
- **Strategist:** Lab L5 OG advances T5 + T6. Export cards are how panels travel to Reddit and group chats.
- **Architect:** Single file touch, 7 response extractors, Satori-safe JSX. No new dependencies.
- **Builder:** Rewrote `/og/[panel]/route.tsx` with live data fetch, agent badge, position pills, watermark. Build + tests pass.
- **Researcher:** Existing intel supports the slice — screenshot culture is the distribution engine.
- **Reality:** PASS — build exit 0, 51 tests pass, TypeScript clean. Data-path fallback verified.

---

## Independent Audits

### Engineering audit (Codex lens)

- **KEEP:** Generic `extractRows()` handles the 7 most common API response shapes without per-panel switch statements. This scales to all 100 panels.
- **KEEP:** `agentForPanel()` from `@razzle/agents` — no metadata duplication.
- **KEEP:** Graceful degradation when API returns empty data or errors — shows panel icon + agent loading copy instead of blank card.
- **REFINE:** The `STAT_CANDIDATE_KEYS` list is static. Future panels with novel stat keys would need the list extended. Low risk for launch-10.
- **KEEP:** Satori constraints fully respected (display:flex, no grid, no unsupported CSS properties).

### Product/brand audit (Opus lens)

- **KEEP:** Design matches DESIGN.md — sand background (#ede0cf), ink borders, position colors (QB blue, RB teal, WR terracotta, TE purple), agent badge, watermark.
- **KEEP:** No "AI" language anywhere. Agent names are staff identities (Hawkeye, Octo, Bones).
- **KEEP:** The card is data-first — player rows with stats front and center. Brand follows from repetition (watermark, colors).
- **KEEP:** Follows the Explore OG pattern (cycle 55) — consistent visual language across share cards.

### Domain check

This work deepens Lab L5 in the Explore → Lab hallway. The OG card preserves panel context (title, blurb, data rows) and agent ownership. The `/lab/{slug}` footer URL provides a direct hallway path back to the in-product panel. Hallway continuity preserved.

---

## KEEP / DELETE / REFINE

- **KEEP:** `extractRows()` generic pattern — proven on Explore, now Lab.
- **KEEP:** Agent badge on OG cards — visual proof of staff ownership.
- **REFINE (follow-up):** Per-player panel OG defaults and production tier bypass.

---

# Daily Build Standup — 2026-05-30 (cont.)

## Cycle 58

> Note: Cycle 57 (Lab L5 OG live data, commit `2e11270`) already shipped and merged
> this morning. The workday was already open. This is a continuation cycle on the
> same date, written as a second section rather than overwriting cycle 57's record.
> Branch protection on `razzle-v2-redesign` was **not verifiable** (GitHub API 403,
> "Resource not accessible by integration") and no run-lock issue could be created
> (read-only `gh`), so this cycle degrades safely: **no automerge**, PR left open
> with `NO_LOCK_GUARDRAIL` noted.

- **Slice:** Lab L2 — "By Staff" grouping toggle in the Lab sidebar
- **PARITY row:** Lab L4–L5 row + connective-tissue hallway checklist (H-04, six agents same ids everywhere)
- **DEPTH layer:** Lab L2 — "20 panels with agent-owned headers + domain loading copy"
- **ACCEPTANCE check:** Gate 2 (Lab — index lists panels, categories visible) + Gate 5 (build/tsc pass)
- **Citation of record:** `docs/v2/AGENTS.md` wiring checklist row "Lab sidebar — group by agent" (was TODO)
- **Why this matters (football-specific):** Managers think in *staff roles*, not feature categories — "who's my trade guy", "who do I trust on injuries", "is this a buy-low". A Lab that browses **by analyst** (Bones = trade leverage, Hawkeye = usage/breakout edges, Dr. Dolphin = injury durability, Octo = projections, Atlas = history) maps directly to the decision the manager is trying to make, and reinforces the six-staff identity the Situation Room and Player Sheet already use. Roster/trade/waiver decisions get a clear "go to this staffer" path.
- **Owner:** Builder
- **Files touched:** `apps/web/components/lab/LabSidebar.tsx`, `packages/ui/styles/lab.css`, `docs/v2/AGENTS.md`
- **Tests / evidence required:** tsc, `npm run build`, eslint, panel-ownership count
- **Not this cycle:** Deepen registry panel ownership (touches `@razzle/agents` source of truth); redesign the Lab index grid; touch pricing/auth.

### Votes

- **Product Strategist:** SHIP — This is the explicit `docs/v2/AGENTS.md` TODO and a clean L2 hallway climb. The staff mental model is how Reddit users actually frame questions; browsing the Lab by analyst is more screenshot-worthy and more on-brand than a flat category list. Bones → Builder: make sure unowned panels don't silently vanish — they must land in a visible catch-all.
- **Engineering Architect:** SHIP — Single component + one CSS block. Grouping reads `agentForPanel()` from `@razzle/agents` (no metadata duplication, honors the "do not duplicate agent metadata" rule). Toggle state is local, search still overrides grouping. Risk: sparse ownership makes the general desk large — acceptable, logged as REFINE. Strategist → Architect: confirm the catch-all is headed by Razzle so it still reads as staff, not "misc." Done.
- **Builder:** SHIP — Surgical. Reused the existing collapsible-section pattern and `SidebarItem`; desk keys are namespaced (`desk-<id>`) so they don't collide with category collapse state. Avatars already exist under `/agents/*.svg`. Builder → Strategist: the general desk is large (86 panels) until registry ownership deepens — honest limitation, not a regression (category view is unchanged and still default).
- **Verdict:** SHIP (3/3)

### Blind-spot callouts

- Architect → Strategist: Registry `labPanels` only maps 14 of 100 panels, so "By Staff" is currently more of a curated-desk view than a full taxonomy. Strategist should queue a registry-ownership deepening slice to make the staff view comprehensive.
- Strategist → Builder: The toggle defaults to "By Category" so existing muscle memory is preserved; good call not to flip the default.
- Builder → Architect: `By Staff` is hidden when the sidebar is collapsed (48px rail) since the segmented labels can't fit — verified the collapse path still renders icons.

### Handoff

Builder implemented. Reality Checker verified with tsc + build + eslint + ownership count. Slice complete. Follow-up: deepen `@razzle/agents` panel ownership so the general desk shrinks.

---

## Build Review — 2026-05-30 (cycle 58)

- **Slice:** Lab L2 — "By Staff" grouping toggle in the Lab sidebar
- **PARITY/DEPTH/ACCEPTANCE claim:** Lab L2 — agent-owned headers; `docs/v2/AGENTS.md` Lab sidebar TODO closed
- **Evidence:**
  - `npm run typecheck --workspace=apps/web` (`tsc --noEmit`) — exit 0, 0 errors
  - `npm run build` (`next build`) — exit 0, 109 static pages, `/lab` + `/lab/[panel]` (100 paths) build clean
  - `npx eslint components/lab/LabSidebar.tsx` — 0 errors (3 pre-existing warnings)
  - Ownership count: all six staff desks ≥1 panel (Razzle 1, Dolphin 1, Hawkeye 3, Bones 3, Octo 4, Atlas 2); general desk 86
  - Evidence file: `docs/v2/evidence/2026-05-30-lab-staff-grouping.md`
- **Layer verification:** PASS — the Lab sidebar now offers agent-owned section headers (avatar + name + role) grouping panels by their registry owner. Matches DEPTH Lab L2 "agent-owned headers" and closes the hallway wiring TODO.
- **Issues found:** Sparse registry ownership → large general desk. Not a regression; category view unchanged and remains default. Visual screenshot deferred (no dev server this cycle).
- **Ship verdict:** PASS
- **Required fixes:** None
- **Follow-up debt:** Deepen `@razzle/agents` `labPanels` ownership; consider per-desk panel counts in headers; visual/responsive screenshot pass.
- **Commit hash:** f5b38ec
- **`git status --porcelain`:** clean

---

## Outside Reality Briefing — 2026-05-30 (cycle 58)

**Data Researcher — light scan (5 min budget):**

1. **Users frame questions by role, not feature.** r/DynastyFF and r/fantasyfootballadvice posts repeatedly ask "who's my trade guy / who do I trust on injuries / is this a buy-low" — a staff mental model, the same instinct the Situation Room agents exploit. Logged to `docs/v2/REDDIT-INTEL.md` (cycle 58 entry).
2. **No new high-signal thread** this scan; the existing screenshot-culture intel still holds. This directly informed the Lab "By Staff" slice — no web scraping needed.

---

## Team Roll Call

- **Razzle / Chief:** Cycle 57 already shipped this morning; ran one bounded continuation slice. No scope sprawl, no automerge (guardrails unverifiable).
- **Strategist:** "By Staff" maps the Lab to how managers actually think — go to the staffer who owns the decision. Closes the hallway TODO.
- **Architect:** One component + one CSS block, registry as single source of truth. Safe touch surface.
- **Builder:** Added a By Category / By Staff toggle; agent-headed desks; Razzle catch-all for the long tail. Build + tsc + eslint green.
- **Researcher:** Reddit signal — users think in staff roles; the staff view matches the mental model.
- **Reality:** PASS — tsc 0 errors, build exit 0, eslint 0 errors, all six desks non-empty.

---

## Independent Audits

### Engineering audit (Codex lens)

- **KEEP:** Grouping uses `agentForPanel()` from `@razzle/agents` — no duplicate agent metadata (honors the "do not duplicate" rule in `docs/v2/AGENTS.md`).
- **KEEP:** Desk collapse keys namespaced (`desk-<id>`) — no collision with category collapse state.
- **KEEP:** Search still overrides grouping (single code path for query results).
- **REFINE:** Registry `labPanels` ownership is sparse (14/100). Deepen ownership so the general desk shrinks. Logged as follow-up debt.
- **KEEP:** No new dependencies; reuses existing `SidebarItem` and collapsible pattern.

### Product/brand audit (Opus lens)

- **KEEP:** DESIGN.md respected — mono font, 2px ink borders, terracotta active state, slightly-sticker segmented toggle. No gradients, no thin borders.
- **KEEP:** No "AI" language. Desks are named staff (Bones, Hawkeye, Dr. Dolphin, Octo, Atlas) with human roles.
- **KEEP:** Catch-all is headed by Razzle "Chief of Staff · the floor" — stays in-character instead of a generic "Other" bucket.
- **REFINE:** A Reddit power user would screenshot a populated staff desk; the large general desk is the weak spot until ownership deepens.

### Domain check

This deepens the Lab pillar and preserves hallway continuity: the same six staff ids from `@razzle/agents` that appear in the Room, Player Sheet Ask, and Bureau headers now organize the Lab. A manager can browse straight to the analyst who owns a roster/trade/injury decision. Hallway H-04 reinforced; no room siloed.

---

## KEEP / DELETE / REFINE (cycle 58)

- **KEEP:** `By Staff` toggle — registry-driven, on-brand, closes a standing hallway TODO.
- **KEEP:** Razzle general desk as catch-all — keeps the staff framing intact.
- **REFINE (follow-up):** Deepen `@razzle/agents` panel ownership; add per-desk counts; visual screenshot pass.
- **DELETE:** None this cycle.
