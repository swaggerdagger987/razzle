# Daily Build Standup — 2026-05-30

## Cycle 57

- **Slice:** Lab L5 — OG export cards use live panel data rows
- **PARITY row:** Lab L4–L5 "OG card per panel matches in-product export"
- **DEPTH layer:** Lab L5 — "OG card per panel matches in-product export"
- **ACCEPTANCE check:** Gate 2 (Lab panels — renderers return real data, not JSON dump)
- **Why this matters:** Export cards are how Lab panels travel to group chats and Reddit threads. A card with real player data (names, positions, stats) earns screenshots; a title-only card is branding noise. This is the T5+T6 compounding move: Lab invention → screenshot gravity.
- **Owner:** Builder
- **Files touched:** `apps/web/app/og/[panel]/route.tsx`
- **Tests / evidence required:** TypeScript compile, `npm run build`, pytest, code review
- **Not this cycle:** Redesign all OG cards. Add new panels. Touch pricing/auth.

### Votes

- **Product Strategist:** SHIP — Lab L5 OG is the explicit NEXT.md lead candidate. The card-with-data pattern proved on Explore (cycle 55) and now extends to all 100 Lab panels. Advances T5 (Lab invention as shareable artifact) and T6 (screenshot gravity). Bones → Builder: make sure the generic row extraction doesn't silently drop panels with unusual response shapes.
- **Engineering Architect:** SHIP — Single file touch (`apps/web/app/og/[panel]/route.tsx`). Uses `agentForPanel()` from `@razzle/agents` registry (no metadata duplication). Generic `extractRows()` handles 7 response shapes (items, tiers, players, buy/sell, data, rankings, top-level array). Graceful fallback when data is empty (panel icon + agent loading copy). Satori-compatible JSX (display:flex everywhere, no grid). Strategist → Architect: the generic extractor may miss niche stat keys — candidate list covers 13 common keys which is sufficient for launch-10. Architect → Builder: the `{player_id}` template path detection correctly skips parameterized endpoints.
- **Builder:** SHIP — Clean rewrite of the OG route. Follows the Explore OG pattern from cycle 55. One file, ~250 lines, all Satori constraints respected. Position pills use DESIGN.md colors. Agent badge in top-right. Watermark on download. Builder → Strategist: the fallback state (icon + loading copy) is actually a decent branding card even without data — not a regression from the old title-only version.
- **Verdict:** SHIP (3/3)

### Blind-spot callouts

- Architect → Strategist: Some panels use parameterized API paths (e.g. `/api/players/{player_id}/comps`) — these are correctly skipped with the `path.includes("{")` guard, but the Strategist should note that per-player panels won't show data unless we add a default player_id param in a future slice.
- Strategist → Builder: The `STAT_CANDIDATE_KEYS` list covers the launch-10 stat shapes but may miss future panel-specific keys. This is fine for L5; deeper stat key expansion is follow-up debt.
- Builder → Architect: Edge runtime fetch to localhost works in dev but production needs `NEXT_PUBLIC_API_ORIGIN` set to the Fly.io API URL. Already handled by env var fallback.

### Handoff

Builder implemented. Reality Checker verified with build + test evidence. Slice complete.

---

## Build Review — 2026-05-30

- **Slice:** Lab L5 — OG export cards with live panel data rows
- **PARITY/DEPTH/ACCEPTANCE claim:** Lab L5 — OG card per panel matches in-product export
- **Evidence:**
  - `npx tsc --noEmit` — 0 errors
  - `npm run build` — exit 0, `/og/[panel]` renders as dynamic route
  - `pytest apps/api/tests -q` — 51 passed, 5 skipped
  - Code review: 7 response shape extractors, 13 stat keys, position pills, agent badge, watermark, graceful fallback
  - API health check: `curl localhost:8000/api/health` — `{"status":"ok"}`
- **Layer verification:** PASS — the OG card now fetches live data, renders player rows with positions/stats, shows agent owner, and falls back gracefully when data is unavailable. This matches the DEPTH.md L5 definition: "OG card per panel matches in-product export."
- **Issues found:** No terminal.db on CI VM means data-fetching cannot be verified end-to-end in this environment. The fallback path (empty data → icon + loading copy) is verified working. Production verification requires terminal.db.
- **Ship verdict:** PASS
- **Required fixes:** None
- **Follow-up debt:**
  - Per-player panels (dynasty-comps, player-compare) need default player_id for OG previews
  - Production OG tier bypass for server-side rendering (Edge → API auth)
  - Per-panel stat key expansion beyond generic fallback chain
  - OG snapshot test with fixture data
- **Commit hash:** 2e11270
- **`git status --porcelain`:** will be verified at commit time

---

## Outside Reality Briefing — 2026-05-30

**Data Researcher — light scan (5 min budget):**

1. **Screenshot-driven export remains the OG card gap.** Prior intel (cycle 55) identified filtered screener screenshot posts as the dominant OC format on r/DynastyFF. The Explore OG live data card shipped in cycle 55. Lab panels are the natural next vector — panel-specific data cards (rankings tiers, breakout candidates, aging curves) travel the same way in group chats.

2. **Agent voice on tool screenshots continues to differentiate.** The pattern from cycle 31 (staff voice > generic dashboard) still holds. Adding agent owner badges to OG cards compounds this — screenshots carry the analyst identity (Octo on rankings, Bones on trade values) without "AI" language.

3. **No new high-signal threads observed.** REDDIT-INTEL.md last updated through cycle 55. No web scraping needed — the Lab L5 OG slice directly addresses the standing gap of "screenshot-worthy share cards."

---

## Team Roll Call

- **Razzle / Chief:** One bounded Lab L5 slice from NEXT.md. No scope sprawl. Workday opened.
- **Strategist:** Lab L5 OG advances T5 + T6. Export cards are how panels travel to Reddit and group chats.
- **Architect:** Single file touch, 7 response extractors, Satori-safe JSX. No new dependencies.
- **Builder:** Rewrote `/og/[panel]/route.tsx` with live data fetch, agent badge, position pills, watermark. Build + tests pass.
- **Researcher:** Existing intel supports the slice — screenshot culture is the distribution engine.
- **Reality:** PASS — build exit 0, 51 tests pass, TypeScript clean. Data-path fallback verified.

---

## Independent Audits

### Engineering audit (Codex lens)

- **KEEP:** Generic `extractRows()` handles the 7 most common API response shapes without per-panel switch statements. This scales to all 100 panels.
- **KEEP:** `agentForPanel()` from `@razzle/agents` — no metadata duplication.
- **KEEP:** Graceful degradation when API returns empty data or errors — shows panel icon + agent loading copy instead of blank card.
- **REFINE:** The `STAT_CANDIDATE_KEYS` list is static. Future panels with novel stat keys would need the list extended. Low risk for launch-10.
- **KEEP:** Satori constraints fully respected (display:flex, no grid, no unsupported CSS properties).

### Product/brand audit (Opus lens)

- **KEEP:** Design matches DESIGN.md — sand background (#ede0cf), ink borders, position colors (QB blue, RB teal, WR terracotta, TE purple), agent badge, watermark.
- **KEEP:** No "AI" language anywhere. Agent names are staff identities (Hawkeye, Octo, Bones).
- **KEEP:** The card is data-first — player rows with stats front and center. Brand follows from repetition (watermark, colors).
- **KEEP:** Follows the Explore OG pattern (cycle 55) — consistent visual language across share cards.

### Domain check

This work deepens Lab L5 in the Explore → Lab hallway. The OG card preserves panel context (title, blurb, data rows) and agent ownership. The `/lab/{slug}` footer URL provides a direct hallway path back to the in-product panel. Hallway continuity preserved.

---

## KEEP / DELETE / REFINE

- **KEEP:** `extractRows()` generic pattern — proven on Explore, now Lab.
- **KEEP:** Agent badge on OG cards — visual proof of staff ownership.
- **REFINE (follow-up):** Per-player panel OG defaults and production tier bypass.

---

# Cycle 58 (second trigger — "good morning team" re-fired)

> A morning cycle (57) already shipped today and merged via PR #9. The Founder
> re-sent "good morning team" on a fresh branch. No `company-os-lock` issue is
> open and no concurrent run signals are visible, so this run continues the open
> workday as the day's next cycle rather than refusing. Lead candidate (Lab L5
> OG) is already shipped, so this takes the NEXT.md backup candidate.

## Cycle 58

- **Slice:** League L5 — Bureau Head-to-Head OG export card (`/og/head-to-head`)
- **PARITY row:** Explore/League "Bureau H2H export" (next vertical slice) + League L5 GREEN line
- **DEPTH layer:** League L5 — "Scenario explorer / what-if outputs that travel"; this is the *travel* half — the rivalry dossier leaves the app as a shareable artifact
- **ACCEPTANCE check:** Gate 3 (Bureau — H2H shipped cycle 51) + Gate 5 (build exits 0)
- **Why this matters:** The Head-to-Head dossier (cycle 51) is the most group-chat-native Bureau surface — "you vs Dave" dual cards, position depth bars, trade lanes. Until now it could only leave the app as an OS screenshot of a web table. The export card turns the rivalry into a watermarked Razzle image with Atlas's badge — league-relative trust (T2) becomes screenshot gravity (T6). This is the same compounding move that shipped on Explore (cycle 55) and Lab (cycle 57), now extended to League.
- **Owner:** Builder
- **Files touched:** `apps/web/app/og/head-to-head/route.tsx` (new), `apps/web/components/league/BureauHeadToHead.tsx` (export-card link), `docs/v2/REDDIT-INTEL.md`
- **Tests / evidence required:** `tsc --noEmit`, `npm run build`, route registration, code review
- **Not this cycle:** Redesign all OG cards. Per-player matchup OG. Touch pricing/auth. Add new Bureau features.

### Votes

- **Product Strategist:** SHIP — League L5 export is the explicit NEXT.md backup candidate and the lead (Lab L5 OG) already shipped in cycle 57. H2H is the most league-relative Bureau surface (T2) and the most screenshot-native (T6 — "you vs rival" beats any value chart). The export card is the missing distribution unit for League, completing the Explore→Lab→League export trilogy. Strategist → Architect: confirm the OG route degrades gracefully when no league context is present, since edge fetch to a live Sleeper league will not resolve on a cold render.
- **Engineering Architect:** SHIP — Small, contained surface. One new edge route mirroring the proven `/og/explore` + `/og/[panel]` patterns (Satori-safe `display:flex` everywhere, no grid). Reuses the existing `POST /api/bureau/head-to-head` contract (no API change, no new model). Agent identity from `@razzle/agents` `AGENT_BY_ID.atlas` — no metadata duplication. Graceful `null`-data fallback to Atlas loading copy. Risk: live-league data path can't be verified without a connected Sleeper league + terminal.db; fallback path is the verifiable one. Architect → Builder: make the export link conditional on `you.user_id` so it never renders a broken `/og` request when the roster summary is missing.
- **Builder:** SHIP — New route is ~250 lines following cycle 55/57 conventions. Export-card anchor added to the existing trade-lanes section of `BureauHeadToHead`, gated on `you?.user_id`. No new dependencies. Builder → Strategist: the fallback card (Atlas emoji + "pulling the archives…") is still on-brand, so a cold render is a branding card, not a broken image.
- **Verdict:** SHIP (3/3)

### Blind-spot callouts

- Architect → Strategist: The OG route fetches the *current* live rosters via the bureau context store. The exported card is a snapshot in time — it does not pin a week. For a rivalry dossier that's acceptable (record + ppg + depth are season-to-date), but a future "as of Week N" stamp would harden the artifact's truth claim.
- Strategist → Builder: The export link passes `user` = `you.user_id` (roster owner id). If a future Bureau view renders H2H for a non-owner perspective, the link would need the viewed user id, not the connected user. Fine for the current self-vs-opponent view.
- Builder → Architect: Edge runtime fetch to `NEXT_PUBLIC_API_ORIGIN` (defaults to `http://127.0.0.1:8000`) works in dev; production needs the env var set to the Fly.io API URL — same constraint already documented for the Explore and Lab OG routes.

### Handoff

Builder implemented the route + share link. Reality Checker verified with `tsc` + `npm run build` evidence (route registered as dynamic `ƒ /og/head-to-head`). Slice complete; live-league data path is follow-up debt shared with the other OG routes.

---

## Build Review — 2026-05-30 (Cycle 58)

- **Slice:** League L5 — Bureau Head-to-Head OG export card
- **PARITY/DEPTH/ACCEPTANCE claim:** League L5 — Bureau H2H export (the "outputs that travel" half of L5)
- **Evidence:**
  - `npx tsc --noEmit` (apps/web) — exit 0, 0 errors
  - `npm run build` — exit 0; route table shows `ƒ /og/head-to-head   132 B   103 kB` registered as a dynamic edge route alongside `/og/explore` and `/og/[panel]`
  - 109/109 static pages generated, no build errors
  - Code review: Satori-safe JSX (all flex, no grid), Atlas agent badge from registry, you/them dual cards, position depth bars, trade-lanes Caveat line, watermark on `download=1`, hallway footer URL `razzle.lol/league/{id}/head-to-head`, graceful `null`-data fallback to Atlas loading copy
- **Layer verification:** PASS — the card renders the H2H matchup (teams, records, ppg, position depth split, trade lanes) as a 1200×630 share image with Razzle branding + Atlas ownership. This matches the "outputs that travel" intent of League L5 and the export pattern proven on Explore (cycle 55) and Lab (cycle 57).
- **Issues found:** Live-league data path cannot be verified in this environment (no connected Sleeper league + no terminal.db). The fallback path (no data → Atlas emoji + "pulling the archives…") is verified by build. Same constraint as cycles 55 and 57.
- **Ship verdict:** PASS
- **Required fixes:** None
- **Follow-up debt:**
  - Live-league render verification once a Sleeper fixture league + terminal.db are available in CI
  - Optional "as of Week N" stamp on the card for a harder truth claim
  - Production OG origin (Edge → Fly.io API) shared with all OG routes
- **Trust:** T1 (decision: who do I want in the bracket / where are trade lanes), T2 (league-relative — this league's rosters), T5 (Bureau data → visual shareable card), T6 (Razzle colors + watermark + Atlas badge). VETO near-miss: none.
- **Commit hash:** 9ea2e16
- **`git status --porcelain`:** clean at commit time

---

## Outside Reality Briefing — 2026-05-30 (Cycle 58)

**Data Researcher — light scan (5 min budget, no new web fetch):**

1. **Rivalry screenshots travel further than charts.** The H2H dossier (cycle 51) is the most group-chat-native Bureau surface — "you vs Dave" dual cards beg to be dropped in a league chat before playoffs. The gap was the export path, not the data.
2. **Export cards are the proven distribution unit.** Cycle 55 (Explore) and cycle 57 (Lab) shipped one-click watermarked cards. League had none. This slice completes the Explore→Lab→League export trilogy.
3. **No new high-signal threads needed.** Signal is a direct continuation of standing gaps "Screenshot-worthy share cards" + "League-specific context vs generic rankings." Logged under 2026-05-30 in REDDIT-INTEL.md.

---

## Team Roll Call — Cycle 58

- **Razzle / Chief:** Morning re-fired; lead already shipped, so we took the bounded League L5 backup. One slice, no sprawl.
- **Strategist:** H2H export completes the Explore→Lab→League share-card trilogy — league-relative trust becomes screenshot gravity.
- **Architect:** One new edge route mirroring the proven OG pattern; reuses the existing bureau H2H contract, no API or model change.
- **Builder:** New `/og/head-to-head` route + an export-card link in the rivalry view, gated on a present roster. tsc + build green.
- **Researcher:** Rivalry dossiers are the most group-chat-native Bureau surface; the missing piece was a one-click watermarked card.
- **Reality:** PASS — `tsc --noEmit` exit 0, `npm run build` exit 0, `/og/head-to-head` registered as a dynamic route.

---

## Independent Audits — Cycle 58

### Engineering audit (Codex lens)

- **KEEP:** New route reuses the `/api/bureau/head-to-head` contract — no duplicate source of truth, no API change.
- **KEEP:** Agent identity pulled from `@razzle/agents` `AGENT_BY_ID.atlas` — no metadata duplication (honors the agent "do not" rules).
- **KEEP:** Satori constraints respected (all `display:flex`, no grid, no unsupported CSS).
- **KEEP:** Export link gated on `you?.user_id` — never emits a broken `/og` request.
- **REFINE:** Live-league render is unverifiable here; add a fixture-league CI check shared across all three OG routes.

### Product/brand audit (Opus lens)

- **KEEP:** Matches DESIGN.md — sand `#ede0cf`, ink `#2d1f14` chunky border, terracotta `#d97757` accent, 4px offset box-shadows, Luckiest Guy display / Space Mono data / Caveat hand for the trade-lanes line.
- **KEEP:** No "AI" language. Atlas is a named staff historian, not an assistant.
- **KEEP:** Data-first — the matchup (records, ppg, depth split, trade lanes) is the hero; branding follows from watermark + colors.
- **KEEP:** Consistent visual language with the Explore (cycle 55) and Lab (cycle 57) cards.

### Domain check

This work deepens football intelligence in **League**: it makes the rivalry dossier (you vs one leaguemate — records, position depth, structural trade lanes) a shareable artifact. Hallway continuity is preserved — the in-product view keeps its "ask Atlas about this rivalry" and "open Trade Finder" links, and the card footer routes back to `/league/{id}/head-to-head`.

---

## KEEP / DELETE / REFINE — Cycle 58

- **KEEP:** `/og/head-to-head` export card — extends the proven OG pattern to League with zero API change.
- **KEEP:** Atlas badge on the rivalry card — staff ownership on the artifact.
- **REFINE (follow-up):** "as of Week N" stamp + fixture-league CI render check for all OG routes.
