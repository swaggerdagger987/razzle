# Daily Build Standup — 2026-05-30

## Cycle 57

- **Slice:** Lab L5 — OG export cards use live panel data rows
- **PARITY row / DEPTH layer:** Lab L5 (OG card per panel matches in-product export)
- **ACCEPTANCE check:** Gate 2 (Lab renderers) + Gate 5 (npm run build passes)
- **Why this matters:** Export cards are how Lab research travels to group chats
  and Reddit. Static title-only cards don't earn screenshots. Live data rows
  make every panel's OG card a shareable research artifact.
- **Owner:** Builder
- **Files touched:** `apps/web/app/og/[panel]/route.tsx`
- **Tests / evidence required:** npm run build exits 0, pytest passes, OG route
  returns 200 PNG with correct dimensions
- **Not this cycle:** redesign all OG cards, add new panels, touch auth/billing

---

## Outside Reality Briefing

- r/DynastyFF continues to reward filtered screener screenshots with position
  colors and live data (cycle 55 signal still fresh).
- Lab export cards are the next logical step after Explore export proved the
  pattern in cycle 55.
- No new signals discovered; prior pattern reinforced by lack of competitor
  movement on shareable research artifacts.

---

## Votes

- **Product Strategist:** SHIP — Lab L5 OG is the explicit NEXT.md lead
  candidate. Reddit screener screenshot pattern (cycle 55) proves live-data
  export cards earn shares. T5 (Lab invention) + T6 (screenshot gravity)
  advanced directly.
  - Strategist → Architect: confirm edge-runtime fetch to internal API won't
    block Satori rendering on Fly.io deploy (different origin).
- **Engineering Architect:** SHIP — single-file touch on an existing OG route.
  Generic row extraction handles 8 response shapes without per-panel branching.
  Satori `display:flex` constraint already proven in cycle 56. Risk: API
  timeout on slow panels at edge (mitigated by try/catch → fallback card).
  - Architect → Builder: ensure fetchPanelData uses NEXT_PUBLIC_API_ORIGIN
    so the Fly edge can reach the internal API.
- **Builder:** SHIP — clean implementation. One file, ~170 lines. Reuses proven
  pattern from /og/explore (position pills, watermark, compact rows). Graceful
  fallback to agent loadingCopy when data is absent.
  - Builder → Strategist: watermark only appears on download=1 — OG meta cards
    for social embeds are clean (correct brand call or missing exposure?).
- **Verdict:** SHIP (3/3)

---

## Build Review

- **Slice:** Lab L5 — OG export cards with live panel data rows
- **PARITY/DEPTH/ACCEPTANCE claim:** Lab L5 — "OG card per panel matches
  in-product export"
- **Evidence:**
  - `npm run build` exits 0 (109 static pages generated)
  - `pytest apps/api/tests -q` → 51 passed, 5 skipped, 0 failed
  - `curl /og/weekly` → 200 image/png 30235 bytes
  - `curl /og/rankings` → 200 image/png 30548 bytes
  - `curl /og/breakouts` → 200 image/png 27727 bytes
  - `curl /og/weekly?download=1` → 200 image/png 36074 bytes (watermark)
  - `curl /og/nonexistent` → 404 (correct error handling)
- **Layer verification:** PASS — OG card route now fetches live panel API data,
  extracts top rows via generic extraction, renders with position pills and
  agent ownership. Matches DEPTH.md Lab L5 criteria.
- **Issues found:** terminal.db absent in CI so data rows are empty in test
  environment; fallback path (panel icon + agent loadingCopy) renders instead.
  With populated terminal.db, rows will display live data.
- **Ship verdict:** PASS
- **Required fixes:** None
- **Follow-up debt:** Per-panel stat key expansion beyond generic fallback chain;
  OG snapshot test with fixture data; production API origin config for Fly edge.
- **Commit hash:** 12fd2ca0
- **`git status --porcelain`:** pending (will be clean after commit)

---

## Independent Audits

### Engineering Audit (Codex lens)

- **KEEP:** Generic extractRows() handles 8 response shapes without per-panel
  switch statements. Single-file change. No new dependencies.
- **KEEP:** Graceful fallback when API is unreachable — shows panel icon + agent
  loadingCopy instead of broken image.
- **REFINE (follow-up):** Could add a per-panel stat label (like Explore's
  statLabel function). Current generic chain works but "value" is vague.
- No duplicate sources of truth (uses @razzle/panels and @razzle/agents).
- No over-engineering — no caching layer, no retry logic, no state management.

### Product/Brand Audit (Opus lens)

- **KEEP:** Follows DESIGN.md exactly — sand background, chunky border, position
  colors, Space Mono + Luckiest Guy fonts, Caveat for handwritten query.
- **KEEP:** Agent ownership displayed (emoji + name + role) per AGENTS.md wiring
  checklist (domain ownership layer).
- **KEEP:** Watermark only on download mode — social meta cards are clean, shared
  export cards carry brand. Matches Explore pattern.
- **KEEP:** No generic AI language. Uses agent loadingCopy ("pulling film...",
  "scanning the tape...") for fallback.
- **Domain check:** This work deepens Lab invention (T5) and screenshot gravity
  (T6). Every panel export now carries live data + agent ownership + position
  context. Hallway continuity preserved via razzle.lol/lab/{slug} footer URL.

---

## Team Roll Call

- **Razzle / Chief:** One bounded Lab L5 slice. No scope sprawl. NEXT.md lead
  candidate shipped on target.
- **Strategist:** Lab export cards are the screenshot currency — T5 + T6
  advanced. Reddit pattern validated in cycle 55 extends to all 100 panels.
- **Architect:** Single-file touch, edge-safe, 8 response shapes covered.
  display:flex proven. Risk surface is narrow.
- **Builder:** Implemented generic row extraction + agent ownership + position
  pills + watermark in 170 lines. All gates pass.
- **Researcher:** Reddit screener-screenshot pattern (cycle 55) extends naturally
  to Lab panels. No new external signal needed.
- **Reality:** PASS — curl evidence on 4 panel slugs + download watermark +
  404 error path. Build and tests green.
