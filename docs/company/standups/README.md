# Company Standups

Daily Build Standup outputs land here as `YYYY-MM-DD.md`.

Format and full procedure: `docs/company/MEETINGS.md` → "Daily Build Standup".

Every standup file ends with a three-equals vote and either a slice handoff or a
`KILL` note. A `KILL` standup is still a useful artifact — the company learns from
rejected ideas.
