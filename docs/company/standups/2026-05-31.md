# Daily Build Standup — 2026-05-31

## Cycle 58 (workday cycle 1)

### Big problem / Epic / Today's atom

- **Big problem:** Lab OG export cards must pass FACTORY-DOD Gate C — player row layout on preview, not loading-copy-only shells.
- **Epic:** Lab L5 — OG cards are always screenshot-ready (3 atoms; see `docs/company/state/current-epic.json`).
- **Today's atom:** `lab-og-demo-rows` — static demo player rows on `/og/[panel]` when live API returns empty.

- **Slice:** Lab L5 — OG panel static demo rows when live API empty
- **PARITY row:** Lab L4–L5 OG card per panel matches in-product export
- **DEPTH layer:** Lab L5
- **ACCEPTANCE check:** FACTORY-DOD Gate C2/C3
- **Why this matters:** Cycle 57 shipped live fetch; CI VMs and cold previews still have no `terminal.db`. H2H already uses demo fallback (PR #12). Lab panels need the same pattern so Reddit screenshots show real rows.
- **Owner:** Builder
- **Files touched:** `apps/web/app/og/[panel]/route.tsx`
- **Not this cycle:** Per-player parameterized OG routes, new panels, auth/billing.

### Votes

- **Product Strategist:** SHIP — closes cycle 57 follow-up debt and FACTORY-DOD Gate C for Lab OG. Advances T5 (Lab invention as artifact) and T6 (screenshot gravity). Next epic atom: per-panel demo tuning.
- **Engineering Architect:** SHIP — one file, mirrors H2H `DEMO_H2H` pattern. `demoRowsForPanel()` + slug overrides for launch-10 shapes. Satori fix: single text child for blurb line.
- **Builder:** SHIP — demo rows when `liveHasRows` false; "sample preview" label; removed loading-only empty state.
- **Verdict:** SHIP (3/3)

### Handoff

Reality Checker to curl PNG ≥40KB on `/og/rankings` and `/og/breakouts` with demo rows visible.

---

## Build Review — 2026-05-31

- **Slice:** Lab L5 — OG panel static demo rows
- **Evidence:**
  - `npm run build --workspace=apps/web` — exit 0
  - `JWT_SECRET=test python3 -m pytest apps/api/tests -q` — 51 passed, 5 skipped
  - `curl http://localhost:3000/og/rankings?download=1` — **200 59509** bytes PNG
  - `curl http://localhost:3000/og/breakouts?download=1` — **200 60649** bytes PNG
  - `file` — PNG 1200×630 for both
- **Layer verification:** PASS — OG cards render six player rows with position pills when API empty; subtitle notes "sample preview".
- **Ship verdict:** PASS
- **Follow-up debt:** Per-player panel default params; production API auth for Edge OG; per-slug demo stat label expansion (atom 2).
- **Commit hash:** 0019814f
- **Trust:** T5, T6

---

## Outside Reality Briefing — 2026-05-31

1. **Panel screenshot culture extends beyond Explore.** Cycle 55–57 proved filtered screener + Lab panel cards travel in group chats. Demo rows let marketing/CI preview without terminal.db.
2. **"Sample preview" honesty matches trust.** Users tolerate sample data when labeled; loading-copy-only cards read as broken tools.
3. No new web scrape — standing REDDIT-INTEL patterns still apply.

---

## Team Roll Call

- **Razzle / Chief:** Factory open 2026-05-31. One atom, one PR. Epic tracks Lab OG screenshot readiness.
- **Strategist:** Gate C unblock — Lab exports now screenshot-ready without live DB.
- **Architect:** H2H demo pattern ported; Satori-safe single-child blurb.
- **Builder:** Demo rows + slug overrides in `/og/[panel]/route.tsx`.
- **Researcher:** Screenshot culture intel unchanged; sample label protects trust.
- **Reality:** PASS — build green, pytest 51 passed, OG PNG ~59KB with 200 status.

---

## KEEP / DELETE / REFINE

- **KEEP:** Demo fallback pattern shared with H2H OG; slug-specific launch-10 rows.
- **DELETE:** Loading-copy-only empty state as "done" for OG panels.
- **REFINE:** Next atom — tune per-panel demo stat labels.

---

## Cycle 59 (workday cycle 2)

### Big problem / Epic / Today's atom

- **Big problem:** Lab OG export cards must pass FACTORY-DOD Gate C — player row layout on preview, not loading-copy-only shells.
- **Epic:** Lab L5 — OG cards are always screenshot-ready (atom 2/3).
- **Today's atom:** `lab-og-panel-specific-demos` — per-panel demo stat labels for all launch-10 slugs.

- **Slice:** Lab L5 — launch-10 OG demo stat columns
- **PARITY row:** Lab L4–L5 OG card per panel matches in-product export
- **DEPTH layer:** Lab L5
- **ACCEPTANCE check:** FACTORY-DOD Gate C2/C3
- **Why this matters:** Atom 1 added generic demo rows; launch-10 panels need stat labels matching in-product shapes (FPTS for weekly/gamelog, Score for prospects/breakouts, Peak Age for aging, Chg for dashboard).
- **Owner:** Builder
- **Files touched:** `apps/web/app/og/[panel]/route.tsx`
- **Not this cycle:** Parameterized player_id OG defaults (atom 3).

### Votes

- **Product Strategist:** SHIP — completes launch-10 OG screenshot parity. Advances T5, T6. Next atom: default player_id params.
- **Engineering Architect:** SHIP — one file, extends `DEMO_ROWS_BY_SLUG` for 6 missing launch-10 slugs. `formatStat` handles signed deltas.
- **Builder:** SHIP — weekly/prospects/tradevalues/gamelog/aging/dashboard demo rows with panel-specific stat labels.
- **Verdict:** SHIP (3/3)

---

## Build Review — Cycle 59

- **Slice:** Lab L5 — launch-10 OG demo stat labels
- **Evidence:**
  - `npm run build --workspace=apps/web` — exit 0
  - `JWT_SECRET=test python3 -m pytest apps/api/tests -q` — 51 passed, 5 skipped
  - `curl /og/weekly?download=1` — 200 63819 bytes PNG
  - `curl /og/tradevalues?download=1` — 200 62488 bytes PNG
  - `curl /og/gamelog?download=1` — 200 58408 bytes PNG
  - `curl /og/prospects`, `/og/aging`, `/og/dashboard` — all 200, PNG ≥57KB
- **Layer verification:** PASS — all 10 launch panels show panel-specific stat column headers on demo OG cards.
- **Ship verdict:** PASS
- **Commit hash:** da09d5b4
- **Trust:** T5, T6

---

## Outside Reality Briefing — Cycle 59

1. **Panel-specific stat labels increase screenshot trust.** Generic "Value" on a weekly heatmap reads wrong; FPTS/Score/Peak Age match how Reddit users talk about each tool type.
2. **Launch-10 completeness unlocks marketing previews.** All 10 panels now droppable in threads without terminal.db.
3. No new web scrape.

---

## Team Roll Call — Cycle 59

- **Razzle / Chief:** Epic atom 2/3 shipped. Factory open, tick continues atom 3.
- **Strategist:** Launch-10 OG demo parity — every panel screenshot-ready with correct stat column.
- **Architect:** One-file extension of DEMO_ROWS_BY_SLUG; signed formatStat for dashboard Chg column.
- **Builder:** Six new slug overrides + formatStat delta prefix.
- **Researcher:** Stat-label honesty matches Reddit panel vocabulary per tool type.
- **Reality:** PASS — build green, pytest 51 passed, OG PNGs 58–64KB.

---

## KEEP / DELETE / REFINE — Cycle 59

- **KEEP:** Slug-keyed demo rows; panel-specific stat labels; signed Chg format.
- **DELETE:** Generic "Value" fallback as only demo label for specialized panels.
- **REFINE:** Atom 3 — default player_id for parameterized OG routes.

---

## Cycle 60 (workday cycle 3)

### Big problem / Epic / Today's atom

- **Big problem:** Lab OG export cards must pass FACTORY-DOD Gate C — player row layout on preview, not loading-copy-only shells.
- **Epic:** Lab L5 — OG cards are always screenshot-ready (atom 3/3 — epic complete).
- **Today's atom:** `lab-og-param-defaults` — default `player_id` for parameterized panel OG routes.

- **Slice:** Lab L5 — OG default player_id params
- **PARITY row:** Lab L4–L5 OG card per panel matches in-product export
- **DEPTH layer:** Lab L5
- **ACCEPTANCE check:** FACTORY-DOD Gate C2/C3
- **Why this matters:** `dynasty-comps` and player-scoped panels were skipped when API paths contained `{player_id}`. Default gsis_id + query override enables screenshot URLs without manual params.
- **Owner:** Builder
- **Files touched:** `apps/web/app/og/[panel]/route.tsx`
- **Not this cycle:** Porting comps API to v2 FastAPI.

### Votes

- **Product Strategist:** SHIP — closes Lab L5 OG epic. Advances T5, T6.
- **Engineering Architect:** SHIP — one file; resolves path templates; injects `player_id` for scoped slugs.
- **Builder:** SHIP — DEFAULT_OG_PLAYER_ID, dynasty-comps demo Match % rows, comps array extract.
- **Verdict:** SHIP (3/3)

---

## Build Review — Cycle 60

- **Slice:** Lab L5 — OG default player_id params
- **Evidence:** `docs/v2/evidence/2026-05-31-lab-og-param-defaults.md`
  - `npm run build --workspace=apps/web` — exit 0
  - `JWT_SECRET=test python3 -m pytest apps/api/tests -q` — 51 passed, 5 skipped
  - `curl /og/dynasty-comps?download=1` — 200 65961 bytes PNG
  - `curl /og/gamelog?download=1` — 200 58408 bytes PNG
- **Layer verification:** PASS — dynasty-comps shows Match % comp rows; gamelog uses default player_id for API attempt + demo fallback.
- **Ship verdict:** PASS
- **Commit hash:** ef908a99
- **Trust:** T5, T6

---

## Outside Reality Briefing — Cycle 60

1. **Parameterized OG URLs must work out of the box.** Reddit shares won't include `player_id`; default Chase gsis_id matches demo row anchor player.
2. **Dynasty comps is the only path-templated launch panel** — resolving `{player_id}` unblocks future v2 comps port without OG rework.
3. No new web scrape.

---

## Team Roll Call — Cycle 60

- **Razzle / Chief:** Epic complete — Lab L5 OG screenshot-ready trilogy shipped.
- **Strategist:** Gate C closed for player-scoped panels; OG links shareable without query hacks.
- **Architect:** Path resolver + PLAYER_SCOPED_SLUGS set; comps extract branch.
- **Builder:** DEFAULT_OG_PLAYER_ID + dynasty-comps demo rows.
- **Researcher:** Default star player (Chase) matches existing demo row names across panels.
- **Reality:** PASS — build green, pytest 51 passed, OG PNGs ≥58KB.

---

## KEEP / DELETE / REFINE — Cycle 60

- **KEEP:** DEFAULT_OG_PLAYER_ID; path template resolver; comps extractRows branch.
- **DELETE:** Blank live fetch when path contained `{player_id}`.
- **REFINE:** Port `/api/players/{id}/comps` to v2 API (FOLLOW-UP).
