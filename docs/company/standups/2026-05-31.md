# Daily Build Standup — 2026-05-31

## Cycle 58 — Workday open (good morning team)

- **Epic:** Lab L5 depth — live OG and export cards (`current-epic.json`, active)
- **Atom:** atom-2 — Lab panel in-product export-card button to `/og/[panel]`
- **Slice:** Lab L5 — every `/lab/[panel]` header gets `copy link` + `export card`
- **PARITY row:** F-Lab-OG — "OG card per panel matches in-product export"
- **DEPTH layer:** Lab L5 — "OG card per panel matches in-product export"
- **ACCEPTANCE check:** Gate 2 (Lab panels render real data) + FACTORY-DOD Gate C (share-card path)
- **Trust pillars:** T5 (Lab invention as shareable artifact), T6 (screenshot gravity)
- **Why this matters:** Cycle 57 made the `/og/[panel]` card show live player rows,
  but nothing in the product linked to it. A great share card with no door is dead
  weight. This atom adds the door — one click from any Lab panel to a downloadable,
  watermarked PNG. This is the compounding distribution move: Lab invention →
  screenshot → Reddit/group-chat → new user.
- **Owner:** Builder
- **Files touched (2):**
  - `apps/web/components/lab/LabShareButton.tsx` (new, ~40 lines)
  - `apps/web/components/lab/PanelCanvas.tsx` (wire button into header)
- **Not this cycle:** Redesign the OG card. Per-player panel OG defaults. Touch
  pricing/auth/billing. New deps.

### Strategist resume note

Epic was active with `next_atom_id=atom-2`. Atom-2's original loose title ("rankings
or explore export live rows") was already covered: the generic `/og/[panel]` route
serves live rows for every panel including rankings (cycle 57), and Explore export
shipped cycle 55. The genuine remaining gap in the "live OG **and export cards**"
epic was reachability — Lab panels had no in-product export control. Atom-2 sharpened
to that. Logged per NEXT.md override rule.

### Votes (three-equals)

- **Product Strategist:** SHIP — completes the export half of the active epic. The
  card-with-data exists; the door does not. Bones: this is the Reddit distribution
  loop, not a silo. Advances T5 + T6.
- **Engineering Architect:** SHIP — 2 files, ≤120 lines, no new deps. `LabShareButton`
  is a near-exact mirror of the verified `ExploreShareButton` (cycle 55). Wired into
  the existing `PanelCanvas` header so it renders on all 100 `/lab/[panel]` routes via
  one edit. Button only emits valid registry slugs, and the OG route 404-guards
  unknown slugs.
- **Builder:** SHIP — clean copy of the proven pattern: `copy link` (clipboard) +
  `export card` (`<a download>` → `/og/{slug}?download=1`). Uses existing `btn-chunky`
  tokens; no CSS changes needed.
- **Verdict:** SHIP (3/3)

### Handoff

Builder implemented. Reality Checker verified with build + curl PNG evidence
(`docs/v2/evidence/2026-05-31-lab-share-button-L5.md`). Slice complete.

---

## Build Review — 2026-05-31

- **Slice:** Lab L5 — in-product export-card button to `/og/[panel]`
- **PARITY/DEPTH/ACCEPTANCE claim:** Lab L5 — OG card per panel reachable in-product
- **Evidence:**
  - `npx tsc --noEmit --project apps/web/tsconfig.json` — 0 errors
  - `npm run build --workspace=apps/web` — exit 0, 109 pages, `/lab/[panel]` SSG (100 slugs)
  - `curl /og/rankings?download=1` — **200, 41,147 bytes**, PNG 1200×630 RGBA (≥40KB Gate C2)
  - `curl /og/screener?download=1` — 200, 37,429 bytes, PNG 1200×630
  - `curl /og/not-a-real-panel` — **404** (registry slug guard)
- **Layer verification:** PASS — Lab panels are now one click from a downloadable
  share card. The card content itself was verified in cycle 57; this slice adds the
  reachability door, mirroring the Explore pattern.
- **Issues found:**
  - terminal.db absent in CI VM → OG card renders branded fallback, not live rows
    (same standing limitation as cycle 57; production fetches real rows).
  - No browser engine in VM → `/lab/[panel]` client subtree hydrates client-side, so
    headless curl cannot assert the rendered button DOM. Verified instead via tsc,
    build, source review vs proven `ExploreShareButton`, and the OG route serving a
    real PNG for the button target.
- **Ship verdict:** PASS
- **Required fixes:** None
- **Follow-up debt:**
  - Per-player panel OG defaults (dynasty-comps, player-compare)
  - Production OG tier bypass for server-side rendering
  - Browser-based screenshot evidence once a browse engine is provisioned in CI
- **Commit hash:** see PR / results.tsv (filled at commit time)

---

## Outside Reality Briefing — 2026-05-31

**Data Researcher — light scan (5 min budget):**

1. **Screenshot distribution remains the standing GTM gap.** `REDDIT-INTEL.md`
   (through cycle 55) documents that dynasty OC posts share filtered, position-colored
   data cards and commenters ask for the tool link. Explore got a one-click export in
   cycle 55; Lab panels had the card (cycle 57) but no button. This atom closes that.
2. **Agent-owned cards continue to differentiate** — the OG card already carries the
   panel's staff owner (Octo, Bones, Hawkeye…). Making it reachable means those
   identities now travel on every screenshot.
3. **No new high-signal threads observed** — no web scraping needed; this slice
   directly serves the existing intel.

---

## Team Roll Call

- **Razzle / Chief:** Workday opened for 2026-05-31. One bounded Lab L5 atom resumed
  from the active epic. No scope sprawl. Run lock: no competing `company-os-lock`
  issue open; `gh` is read-only on this VM so the lock issue could not be created —
  proceeded since no other run held it (noted as harness limitation).
- **Strategist:** Sharpened atom-2 to the real export-reachability gap. T5 + T6.
- **Architect:** 2 files, no deps, mirrors verified `ExploreShareButton`. One header
  edit reaches all 100 panels.
- **Builder:** Added `LabShareButton`, wired into `PanelCanvas` header.
- **Researcher:** Existing Reddit intel supports the slice — distribution loop.
- **Reality:** PASS — tsc 0 errors, build exit 0, `/og/rankings?download=1` → 200 41KB
  PNG, invalid slug → 404. Limitations documented honestly (terminal.db, no browser).

---

## KEEP / DELETE / REFINE

- **KEEP:** `LabShareButton` mirrors `ExploreShareButton` — one share-control pattern
  across Explore and Lab.
- **KEEP:** Single `PanelCanvas` header edit reaches all 100 panels.
- **REFINE (follow-up):** Per-player panel OG defaults; production OG tier bypass;
  browser screenshot evidence in CI.
