{
  "date": "2026-05-31",
  "big_problem": "Lab OG share cards must render the same live panel rows users see in-product — not legacy API paths or demo-only shells when terminal.db has tape (DEPTH Lab L5).",
  "epic": "Lab L5 — OG cards match in-product live panel rows",
  "slice_title": "OG fetch via /api/panels/{slug} with live tape label",
  "parity_ref": "PARITY Explore L5 — Lab panel OG data rows",
  "depth_ref": "Lab L5",
  "acceptance_ref": "FACTORY-DOD Gate C — /og/rankings and /og/dashboard PNG ≥40KB",
  "trust_pillars": ["T5", "T6"],
  "allowed_paths": [
    "apps/web/app/og/[panel]/route.tsx"
  ],
  "max_files": 1,
  "max_lines": 80,
  "acceptance_commands": [
    "npm run build --workspace=apps/web",
    "JWT_SECRET=test python3 -m pytest apps/api/tests -q",
    "curl -s -o /tmp/og-rankings.png -w '%{http_code} %{size_download}' 'http://localhost:3000/og/rankings?download=1'",
    "curl -s -o /tmp/og-dashboard.png -w '%{http_code} %{size_download}' 'http://localhost:3000/og/dashboard?download=1'"
  ],
  "out_of_scope": [
    "Renderer LabOgExportLink migrations",
    "NORTH_STAR / DESIGN edits",
    "New npm deps"
  ],
  "builder_mode": "composer-surgical"
}
