{
  "date": "2026-05-31",
  "big_problem": "Lab OG share cards must render the same player rows as in-product panels when the API returns data — not generic demo shells (DEPTH Lab L5).",
  "epic": "Lab L5 — OG live panel row parity",
  "slice_title": "Launch-10 OG cards drop sample label when live rows render",
  "parity_ref": "Lab L5 — OG card per panel matches in-product export",
  "depth_ref": "Lab L5",
  "acceptance_ref": "FACTORY-DOD Gate C — /og/rankings and /og/breakouts PNG ≥40KB",
  "trust_pillars": ["T1", "T5", "T6"],
  "allowed_paths": [
    "apps/web/app/og/[panel]/route.tsx"
  ],
  "max_files": 1,
  "max_lines": 60,
  "acceptance_commands": [
    "npm run build --workspace=apps/web",
    "JWT_SECRET=test python3 -m pytest apps/api/tests -q",
    "curl -s -o /tmp/og-rankings-live.png -w '%{http_code} %{size_download}' 'http://localhost:3000/og/rankings?download=1'",
    "curl -s -o /tmp/og-breakouts-live.png -w '%{http_code} %{size_download}' 'http://localhost:3000/og/breakouts?download=1'",
    "curl -s -o /tmp/og-weekly-demo.png -w '%{http_code} %{size_download}' 'http://localhost:3000/og/weekly?download=1'"
  ],
  "out_of_scope": [
    "New panels",
    "PanelRenderer changes",
    "NORTH_STAR / DESIGN edits"
  ],
  "builder_mode": "composer-surgical"
}
