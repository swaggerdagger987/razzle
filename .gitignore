# OS
.DS_Store
Thumbs.db
Desktop.ini

# Credentials (never commit)
walker-credentials.json

# SQLite WAL files (NEVER commit — causes corruption when git touches them)
*.db-shm
*.db-wal

# Editor
.vscode/
.idea/
*.swp
*.swo

# Python
__pycache__/
*.pyc
*.pyo
venv/
.venv/

# NOTE: .env is intentionally tracked. It carries the server-side OpenRouter
# key used by the Elite-tier LLM proxy. No credit card is attached to the key.

# Data
data/*.db
data/*.sqlite

# Node (if ever needed)
node_modules/

# Build artifacts
frontend/dist/

# Junk
users.db
.pytest_cache/
=*

# Claude
.claude/
data/terminal.db-shm
data/terminal.db-wal
.gstack/
