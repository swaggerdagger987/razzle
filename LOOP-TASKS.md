# Razzle Loop — Phase 24 Task List

> Auto-generated by Razzle Loop. Updated as tasks progress.

**Current Phase**: 24 — Saved Screener Views
**Exit Criterion**: Lab screener has a "Views" button in the toolbar that opens a Saved Views overlay. Users can name and save their current screener configuration (universe, position, columns, filters, sort, season, relevance) to localStorage. Saved views display with universe badge, position badge, filter count, and date. Click to load restores full state and refreshes data. Delete removes the view. Follows Razzle design system. Deployed to Render.

---

## Task 1: Saved Views system (save/load/delete + UI)
**Requirement**: "Add a 'Views' button to the Lab toolbar. Opens an overlay where users can save the current screener state with a custom name, browse saved views, load a view (restoring full state), and delete views. Saved to localStorage as razzle_saved_views. Each view card shows name, universe badge, position badge, filter count, and date. Razzle design: chunky borders, offset shadows, hover lift, sand bg, correct fonts."
**Accept when**: Views button visible in toolbar. Save captures current state. Load restores state and refreshes. Delete removes view. Cards show correct metadata. Design follows DESIGN.md.
**Depends on**: none
**Size**: M
**Status**: PASS
**Attempt**: 1/3
**Result**: Views button added to toolbar between Charts and Tiers. savedViewsOverlay with save input + list. saveCurrentView() captures universe/position/columns/filters/sort/season/relevance. loadSavedView() restores full state, syncs UI controls (position chips, search, relevance, universe), calls applyUniverseUI + populateSeasonSelect + fetchData. deleteSavedView() removes by id. renderSavedViewsList() shows cards with universe badge (NFL orange/PROSP blue/CFB blue), position badge (pos colors), filter count, date. Hover lift on cards. Chunky borders, offset shadows, Caveat annotation. Empty state message.

## Task 2: Deploy + smoke test
**Requirement**: "Verify all JS files pass syntax check. No Python import errors. All HTML pages present. Views button works. Push to master for Render auto-deploy."
**Accept when**: All JS syntax clean. Python imports clean. Views button visible. Pushed to master.
**Depends on**: Task 1
**Size**: S
**Status**: PASS
**Attempt**: 1/3
**Result**: lab.js, app.js, charts.js pass node -c syntax check. Python backend.server imports clean. Views button in toolbar. savedViewsOverlay HTML well-formed. All element IDs match between HTML and JS. Committed and pushed to master.

---

## Loop State
```
Current Phase: 24
Current Task: 2
Current Stage: PASS
Attempt: 1/3
Tasks Completed: 2/2
Loop Iterations: 2
```
