# Razzle Loop — Phase 27 Task List

> Auto-generated by Razzle Loop. Updated as tasks progress.

**Current Phase**: 27 — Aging Curves
**Exit Criterion**: Lab screener has "Aging Curves" button (NFL mode only) that opens a chart overlay. Backend endpoint returns position-average PPG by age and individual top-player career arcs. Canvas chart plots position baseline curve (thick, position-colored) with labeled age axis. Users can overlay individual players (click to toggle). Position selector (QB/RB/WR/TE). Canvas PNG export with Razzle watermark. Follows Razzle design system. Deployed to Render.

---

## Task 1: Backend /api/aging-curves endpoint
**Requirement**: "Add GET /api/aging-curves?position=WR endpoint to server.py + live_data.py. Queries player_week_stats joined with players table (age, position). Groups all player-seasons by position and age, computes average PPG per age bucket. Also returns top 10 individual player career arcs (name, ages[], ppg_values[]). Returns JSON: { baseline: [{age, avg_ppg, count}], players: [{name, team, points: [{age, ppg}]}] }. Only skill positions (QB/RB/WR/TE). Minimum 5 player-seasons per age bucket to include in baseline."
**Accept when**:
- Endpoint returns valid JSON with baseline and players arrays
- Baseline has avg_ppg per age with sample count
- Players array has top 10 by career PPG with per-age data points
- Minimum sample size filter applied (5+ player-seasons per age)
- Works for all 4 positions (QB/RB/WR/TE)
**Depends on**: none
**Size**: M
**Status**: PASS
**Attempt**: 1/3
**Result**: GET /api/aging-curves?position={QB|RB|WR|TE} added to server.py + live_data.py. fetch_aging_curves() queries player_week_stats JOIN players, groups by player_id + season, computes age_at_season = round(current_age - (latest_season - season)). Baseline: avg PPG per integer age bucket (min 5 player-seasons). Players: top 10 by career PPG with min 2 seasons. QB: 16 age buckets (24-40), RB: 12 (23-34), WR: 14 (23-36), TE: 14 (23-36). Top players: Josh Allen (QB 24.2), CMC (RB 21.4), Davante Adams (WR 19.8), Kelce (TE 17.5).

## Task 2: Aging curve chart UI + player overlay
**Requirement**: "Add 'Aging Curves' button to Lab toolbar (NFL mode only, next to Trade Values). Opens a 960px overlay with position selector tabs (QB/RB/WR/TE). Canvas chart: X-axis = age (19-38), Y-axis = PPG (auto-scaled). Position baseline as thick colored line with data point dots. Individual player curves as thin lines (click player name in legend to toggle). Legend shows top 10 players with checkbox toggles. Caveat annotation ('how players age at each position...'). Razzle design: sand bg, chunky border chart frame, position colors, Space Mono axis labels."
**Accept when**:
- Aging Curves button visible in NFL mode, hidden in other modes
- Overlay opens with position tabs that switch data
- Canvas chart renders baseline aging curve with correct data
- Player legend with toggleable individual curves
- Chart has proper axes, labels, grid lines
- Follows Razzle design system
**Depends on**: Task 1
**Size**: L
**Status**: PASS
**Attempt**: 1/3
**Result**: "Aging Curves" button (id=agingCurvesBtn, green border) in Lab toolbar, visible only in NFL mode. 960px overlay with position tabs (QB/RB/WR/TE). Canvas chart: auto-scaled axes, dashed grid lines, Space Mono labels, age on X-axis, PPG on Y-axis. Position baseline as thick (3.5px) position-colored line with bordered data dots. Individual player curves as dashed thin lines with last-name labels. Top 3 players enabled by default. Legend with toggleable pill buttons (10 player colors). Luckiest Guy title + Caveat subtitle. Chunky 2px chart border. Sand background. 8 functions: openAgingCurves, closeAgingCurves, renderACPositionBtns, loadAgingCurves, renderAgingCurveChart, renderACLegend, toggleACPlayer, exportAgingCurvesPNG.

## Task 3: PNG export + deploy + smoke test
**Requirement**: "Add 'Export PNG' button to aging curves overlay. Canvas export: title, position label, aging curve chart, player legend, Razzle watermark. 800px wide. Downloads as razzle-aging-curves-{position}.png. Verify all JS passes node -c syntax check. All Python imports clean. All HTML element IDs match JS. Commit and push."
**Accept when**:
- Export PNG button in overlay
- Canvas renders aging curve with watermark
- Downloads with correct filename
- All JS passes syntax check
- All Python imports clean
- Committed and pushed to master
**Depends on**: Task 2
**Size**: M
**Status**: PASS
**Attempt**: 1/3
**Result**: Export PNG button in overlay header. exportAgingCurvesPNG() renders 800x520 canvas reusing renderAgingCurveChart(targetCanvas), adds watermark. Downloads as razzle-aging-curves-{position}.png. All 7 JS files pass node -c syntax check. Python backend imports clean. All 7 HTML element IDs match JS references (acCanvas, acLegend, acPositionBtns, acChartContainer, acExportBtn, agingCurvesBtn, agingCurvesOverlay). 13 frontend files present.

---

## Loop State
```
Current Phase: 27
Current Task: 3
Current Stage: PASS
Attempt: 1/3
Tasks Completed: 3/3
Loop Iterations: 1
```
