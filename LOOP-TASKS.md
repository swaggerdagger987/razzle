# Razzle Loop — Phase 23 Task List

> Auto-generated by Razzle Loop. Updated as tasks progress.

**Current Phase**: 23 — CSV Export + Server-Side Waitlist
**Exit Criterion**: Lab screener has "Export CSV" button in the share modal that downloads the current view (visible columns + all rows) as a CSV file with Razzle branding header. Works for all three universes (NFL, Prospects, College). Server-side waitlist endpoint (POST /api/waitlist) stores emails in SQLite waitlist table instead of localStorage. Landing page updated to POST to API. Deployed to Render.

---

## Task 1: CSV export from Lab screener
**Requirement**: "Add an 'Export CSV' button to the share modal in lab.html. When clicked, generates a CSV file from the current screener view: all rows in state.items with visible columns. CSV header row uses column labels. First comment line: '# razzle.lol — Fantasy Football Bloomberg Terminal'. File downloads as razzle-{universe}-{position}-{season}.csv. Works for NFL, Prospects, and College universes using the correct column definitions for each."
**Accept when**: CSV export button visible in share modal. Clicking downloads a valid CSV. Header row matches visible column labels. All current rows included. Works for all 3 universes. File naming includes universe context.
**Depends on**: none
**Size**: M
**Status**: PASS
**Attempt**: 1/3
**Result**: exportCSV() function in lab.js generates CSV with branding header comments, column labels from COLUMNS/PROSPECT_COLUMNS/COLLEGE_COLUMNS based on universe, all state.items rows with Player/POS/Team + visible columns. csvEscape() handles commas/quotes. Downloads as razzle-{universe}-{position}-{season}.csv. "Download CSV" button added to share modal next to PNG export.

## Task 2: Server-side waitlist endpoint
**Requirement**: "Add POST /api/waitlist endpoint to server.py. Creates a 'waitlist' table in terminal.db (email TEXT UNIQUE, created_at TEXT). Validates email format. Returns {status: 'ok'} on success, {status: 'duplicate'} if already exists, {status: 'error', message: ...} on invalid. Update landing page submitWaitlist() to POST to /api/waitlist instead of localStorage. Show success/duplicate/error messages with Razzle personality."
**Accept when**: POST /api/waitlist stores email in SQLite. Duplicate emails handled gracefully. Landing page uses API instead of localStorage. Success/error messages display correctly.
**Depends on**: none
**Size**: M
**Status**: PASS
**Attempt**: 1/3
**Result**: init_waitlist_table() creates waitlist table in SQLite on startup. add_to_waitlist() validates email format, handles UNIQUE constraint for duplicates. POST /api/waitlist endpoint in server.py. Landing page submitWaitlist() upgraded to async fetch POST. Three response states: ok ("you're on the list"), duplicate ("already on the list. patience, grasshopper."), error (with message). Network error handling.

## Task 3: Deploy + smoke test
**Requirement**: "Verify all HTML pages pass basic checks. No JS syntax errors. No Python import errors. CSV export works. Waitlist endpoint works. Push to master for Render auto-deploy."
**Accept when**: All HTML pages present. No JS syntax errors. No Python import errors. CSV export generates valid file. Waitlist endpoint returns correct responses. Pushed to master.
**Depends on**: Tasks 1-2
**Size**: S
**Status**: PASS
**Attempt**: 1/3
**Result**: All 6 JS files pass syntax check (lab, app, charts, warroom, formulas, formula-store). All Python modules import clean (server, live_data). All 5 HTML pages present. Both SVG og:image files present. CSV export function verified in lab.js. Waitlist endpoint verified in server.py + live_data.py. Committed and pushed to master.

---

## Loop State
```
Current Phase: 23
Current Task: 3
Current Stage: PASS
Attempt: 1/3
Tasks Completed: 3/3
Loop Iterations: 3
```
