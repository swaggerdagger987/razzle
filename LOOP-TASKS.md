# Razzle Loop — Phase 26 Task List

> Auto-generated by Razzle Loop. Updated as tasks progress.

**Current Phase**: 26 — Trade Value Chart
**Exit Criterion**: Lab screener has "Trade Values" button (NFL mode only) that opens a visual trade value chart. Players grouped by position and DVS tier (Elite/Star/Starter/Bench). Each player card shows name, position badge, team, DVS score, and trade value (1-100 scale). Position filter tabs (ALL/QB/RB/WR/TE). Exportable as PNG with Razzle watermark. Trade Calculator: select players for each side, compare total trade value, green/red balance indicator. Follows Razzle design system. Deployed to Render.

---

## Task 1: Trade Value Chart overlay UI + data fetch
**Requirement**: "Add a 'Trade Values' button to the Lab toolbar (visible only in NFL mode, next to Export Rankings). Clicking opens a full-screen overlay with position filter tabs (ALL/QB/RB/WR/TE). Fetch top players from API, compute DVS client-side, convert to trade values (TV = DVS on 0-100 scale, rounded). Group players by tier: Elite (85+), Star (70-84), Starter (55-69), Bench (<55). Display as a visual grid: tier band headers with rotated sticker badges, player cards with position-colored left stripe, name, team, age, DVS bar, trade value number. Razzle design system: sand bg, chunky 3px borders, offset shadows, Space Mono data, Caveat annotations."
**Accept when**:
- "Trade Values" button visible in NFL mode toolbar, hidden in Prospect/College mode
- Overlay opens with position filter tabs that update the view
- Players fetched from API, DVS computed client-side, trade values derived
- Players grouped by tier (Elite/Star/Starter/Bench) with sticker badges
- Player cards show name, position badge, team, age, DVS/TV
- Follows Razzle design system (sand bg, chunky borders, offset shadows)
- Close button works (click overlay backdrop or X)
**Depends on**: none
**Size**: L
**Status**: PASS
**Attempt**: 1/3
**Result**: Added "Trade Values" button (id=tradeValuesBtn) to Lab toolbar with orange border, visible only in NFL mode. Overlay (960px, max-height 90vh) with position filter tabs (ALL/QB/RB/WR/TE). loadTradeValues() fetches top 200 fantasy-relevant players, computes DVS client-side via computeClientDVS(), rounds to trade value (0-100). renderTradeValueChart() groups by tier (Elite 85+/Star 70-84/Starter 55-69/Bench <55) with rotated sticker badges, player cards with position-colored left stripe, name, position badge, team, age, trade value bar + number. Razzle design: sand bg-card, chunky 2px borders, 2px 2px offset shadows, Space Mono data, Caveat annotations.

## Task 2: Trade Value Chart PNG export
**Requirement**: "Add 'Export PNG' button to trade value chart overlay. Canvas-rendered export: title, tier sections with player rows (position badge, name, team, trade value bar), Razzle watermark. 800px wide portrait layout. Downloads as razzle-trade-values-{position}.png."
**Accept when**:
- Export PNG button in trade value chart overlay
- Canvas renders all tiers and players with correct styling
- Watermark baked into export
- Downloads with correct filename
- Looks good as a Reddit screenshot
**Depends on**: Task 1
**Size**: M
**Status**: PASS
**Attempt**: 1/3
**Result**: Export PNG button in overlay header. exportTradeValuesPNG() renders 800px canvas: sand bg, bold title + Caveat subtitle, tier sections with rotated badge stickers, player rows (position badge, name, team+age, trade value bar with position color, value number). Razzle watermark at bottom. Downloads as razzle-trade-values-{position}.png.

## Task 3: Trade Calculator (two-side value comparison)
**Requirement**: "Add 'Trade Calculator' section below the trade value chart. Two columns: 'Side A' and 'Side B'. Each side has a search input (autocomplete from loaded players) and a list of selected players with their trade values. Total trade value per side shown at bottom. Balance indicator: green check if within 10%, red warning if imbalanced, with the difference shown. 'Clear' button per side. Razzle design: sand card bg, chunky borders, position-colored player chips."
**Accept when**:
- Trade calculator section visible in trade value chart overlay
- Search/autocomplete adds players to Side A or Side B
- Each player shows name + position badge + trade value
- Total value per side computed and displayed
- Balance indicator shows fair/unfair with difference
- Clear button removes all players from a side
- Follows Razzle design system
**Depends on**: Task 1
**Size**: M
**Status**: PASS
**Attempt**: 1/3
**Result**: Trade Calculator section below chart (hidden until data loads). 3-column grid: Side A | Balance | Side B. Each side: search input with 150ms debounced autocomplete from loaded players (top 8 matches), player chips with position badge + name + TV + remove button, total value display. Balance indicator: "FAIR" (green, ≤10% diff) or "A+N"/"B+N" (red, >10% diff). Clear button per side. Razzle design: chunky borders, position-colored chips, Caveat hint text.

## Task 4: Deploy + smoke test
**Requirement**: "Verify all JS passes syntax check. All Python imports clean. Trade Values button visible in NFL mode, hidden in other modes. Overlay opens and loads data. PNG export works. Trade calculator computes correctly. Push to master."
**Accept when**: All JS passes node -c syntax check. Trade Values button correctly visibility-toggled. Overlay renders. PNG exports. Calculator works. Committed and pushed.
**Depends on**: Tasks 1-3
**Size**: S
**Status**: PASS
**Attempt**: 1/3
**Result**: All 4 JS files pass node -c syntax check (lab.js, app.js, charts.js, warroom.js). Python backend.server imports clean. Trade Values button visible in NFL mode (universe === "nfl"), hidden in Prospect/College modes. All 13 trade value functions defined. All HTML element IDs match JS references. All frontend files present (5 HTML, 7 JS, 1 CSS, 1 SVG). Committed and pushed to master.

---

## Loop State
```
Current Phase: 26
Current Task: 4
Current Stage: PASS
Attempt: 1/3
Tasks Completed: 4/4
Loop Iterations: 1
```
