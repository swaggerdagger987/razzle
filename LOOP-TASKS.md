# Razzle Loop — Phase 28 Task List

> Auto-generated by Razzle Loop. Updated as tasks progress.

**Current Phase**: 28 — Positional Heat Maps
**Exit Criterion**: Lab screener has "Heat Map" button (NFL mode only) that opens a canvas-rendered heat map overlay. Backend endpoint GET /api/heatmap?position={QB|RB|WR|TE}&group={production|efficiency|usage} returns top 25 players with percentile rankings across key stats. Canvas renders a dense color-coded grid: rows = players, columns = stats, cells colored green (elite) to red (poor) by percentile. Position selector tabs. Stat group presets (Production, Efficiency, Usage). Player names in rows, stat labels in columns. Canvas PNG export with Razzle watermark. Follows Razzle design system. Deployed to Render.

---

## Task 1: Backend /api/heatmap endpoint
**Requirement**: "Add GET /api/heatmap?position=WR&group=production endpoint to server.py + live_data.py."
**Accept when**:
- Endpoint returns valid JSON with players array and stat metadata
- Each player has raw value and percentile (0-100) for each stat
- Percentiles computed correctly within position group
- Top 25 players by PPG for relevance filtering
- Position-specific stat sets (QB/RB/WR/TE each get relevant stats)
- Works for all 4 positions with all 3 stat groups
**Depends on**: none
**Size**: M
**Status**: PASS
**Attempt**: 1/3
**Result**: GET /api/heatmap?position={QB|RB|WR|TE}&group={production|efficiency|usage} added to server.py + live_data.py. fetch_heatmap() queries player_week_stats JOIN players, aggregates season stats, computes derived metrics (YPC, CMP%, catch_rate, etc.), enriches with rate metrics from player_week_metrics. Percentiles computed within full position group (not just top 25). Position-specific stat sets: QB gets passing stats, RB rushing, WR/TE receiving. "Higher is better" inversion for negative stats (TO, INT%). Tested: QB Lamar 99.2 PPG pctile, WR Chase 99.7, RB Barkley 99.5, TE Bowers 98.4.

## Task 2: Heat map UI overlay + canvas rendering
**Requirement**: "Add 'Heat Map' button to Lab toolbar (NFL mode only). Opens a 980px overlay with position/group selectors. Canvas renders color-coded percentile grid."
**Accept when**:
- Heat Map button visible in NFL mode, hidden in other modes
- Overlay opens with position tabs and stat group selector
- Canvas renders color-coded grid with correct data
- Color scale: red to yellow to green by percentile
- Player names and stat labels readable
- Stat group switching reloads data
- Follows Razzle design system
**Depends on**: Task 1
**Size**: L
**Status**: PASS
**Attempt**: 1/3
**Result**: Heat Map button (id=heatMapBtn, red border) in Lab toolbar, visible only in NFL mode. 1000px overlay with position tabs (QB/RB/WR/TE) and stat group buttons (Production/Efficiency/Usage). Canvas chart: auto-sized grid, player names with team badge in left column, rotated stat labels in header. Cells colored by percentile (red #e63946 at 0, yellow #ffc857 at 50, green #2ec4b6 at 100). Raw values displayed in cells. Alternating row bg. Luckiest Guy title + Caveat subtitle. Space Mono data values. Chart border 2px. Color legend bar below chart. 9 functions: openHeatMap, closeHeatMap, renderHMPositionBtns, renderHMGroupBtns, heatColor, textColorForBg, loadHeatMap, renderHeatMapChart, exportHeatMapPNG.

## Task 3: PNG export + deploy + smoke test
**Requirement**: "Add 'Export PNG' button to heat map overlay. Canvas export with watermark. Verify syntax. Commit and push."
**Accept when**:
- Export PNG button in overlay
- Canvas renders heat map with watermark
- Downloads with correct filename
- All JS passes syntax check
- All Python imports clean
- Committed and pushed to master
**Depends on**: Task 2
**Size**: M
**Status**: PASS
**Attempt**: 1/3
**Result**: Export PNG button in overlay header. exportHeatMapPNG() renders canvas reusing renderHeatMapChart(targetCanvas), adds watermark. Downloads as razzle-heatmap-{position}-{group}.png. All 6 JS files pass node -c syntax check. Python backend imports clean. All 8 HTML element IDs match JS references. 17 frontend files present. Committed and pushed.

---

## Loop State
```
Current Phase: 28
Current Task: 3
Current Stage: PASS
Attempt: 1/3
Tasks Completed: 3/3
Loop Iterations: 1
```
