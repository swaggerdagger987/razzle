# Razzle Loop — Phase 30 Task List

> Auto-generated by Razzle Loop. Updated as tasks progress.

**Current Phase**: 30 — Lab Mobile + Social Previews + Reddit-Ready Polish
**Exit Criterion**: Lab screener is fully usable on mobile (responsive table with horizontal scroll, touch-friendly filters, readable on 375px width). Shared URLs generate proper OG preview cards with dynamic title/description based on screener state. Chart overlays are mobile-friendly (scrollable, closeable). Profile popup works on mobile. Landing page hero CTA is prominent. All pages pass basic mobile viewport test. Deployed to Render.

---

## Task 1: Lab screener mobile responsive layout
**Requirement**: "Make the Lab screener (lab.html) fully usable on mobile viewports (375px-768px). Key changes: (a) Filter bar wraps gracefully — position chips, team filter, search, min GP should stack vertically on narrow screens. (b) Toolbar buttons (Presets, Columns, Views, Charts, etc.) wrap into a 2-row or scrollable horizontal bar on mobile. (c) Table container keeps horizontal scroll but gets proper touch momentum scrolling (-webkit-overflow-scrolling: touch). (d) Sticky player name column works on mobile (narrower width, smaller font if needed). (e) Column picker overlay is full-screen on mobile instead of a dropdown. (f) Player count and sort indicator remain visible. (g) No horizontal page overflow — only the table scrolls horizontally, not the whole page. Add media queries to styles.css for breakpoints at 768px and 480px."
**Accept when**:
- Lab loads correctly on 375px viewport width
- Filter bar stacks vertically without overflow
- Toolbar buttons are accessible (wrap or scroll)
- Table scrolls horizontally with touch momentum
- Player name column stays sticky on mobile
- No page-level horizontal overflow
- Column picker is usable on mobile
**Depends on**: none
**Size**: L
**Status**: PASS
**Attempt**: 1/3
**Result**: Added comprehensive mobile CSS at 768px and 480px breakpoints. Toolbar: horizontal scroll with hidden scrollbar + touch momentum. Table: -webkit-overflow-scrolling: touch. Page: overflow-x: hidden on html/body. All overlays (column picker, profile, charts, trade values, aging curves, heat map, share, rankings, saved views): full viewport width on mobile with no border-radius/shadow. Column picker: full-screen. Filter bar: stacks on 480px (filter-group width:100%). Chips: larger tap targets (min-height:32px). Profile: stacks vertically on 480px. Footer: centered on mobile. Chart canvas: width:100% + auto height.

## Task 2: Chart overlays mobile-friendly
**Requirement**: "Make all chart overlays (Radar, Scatter, Trend, Compare, Trade Values, Aging Curves, Heat Map) usable on mobile: (a) Overlays take full viewport width on mobile (max-width: 100vw, no side padding). (b) Close button (X) is large and touch-friendly (min 44px tap target). (c) Canvas charts scale down proportionally — use CSS max-width: 100% on canvas elements. (d) Chart control dropdowns (player selectors, stat selectors) stack vertically on mobile. (e) Export PNG button remains accessible. (f) Scroll within overlay if content exceeds viewport height."
**Accept when**:
- All chart overlays open and display correctly on 375px viewport
- Charts are readable (not cut off or overflowing)
- Close button is easily tappable
- Dropdowns/controls are usable on mobile
- Overlays scroll if content is taller than viewport
**Depends on**: none
**Size**: M
**Status**: PASS
**Attempt**: 1/3
**Result**: All chart overlays (Radar, Scatter, Trend, Compare, Trade Values, Aging Curves, Heat Map) get full-viewport width on mobile via !important overrides on inline-styled divs. Canvas elements scale to 100% width with auto height. Chart tab buttons wrap. Close buttons get 44px min tap targets. Compare radar canvas also scales. All overlay divs get overflow-y: auto for scrolling.

## Task 3: Player profile popup mobile layout
**Requirement**: "The player profile popup (triggered by clicking player name) needs mobile treatment: (a) On mobile, profile takes full viewport width and height (position: fixed, inset: 0). (b) Close button is prominent and touch-friendly. (c) Multi-season career log table scrolls horizontally within the popup. (d) Profile header (name, team, age, draft info) wraps gracefully. (e) Combine data section stacks vertically. (f) Add a swipe-down-to-close gesture or prominent close bar at top."
**Accept when**:
- Profile popup opens full-screen on mobile
- Career log table scrolls horizontally
- Close button is easily tappable
- All profile content is readable on 375px
- No content overflow or cutoff
**Depends on**: none
**Size**: M
**Status**: PENDING
**Attempt**: 0/3

## Task 4: Dynamic OG meta tags for shared URLs
**Requirement**: "When a Lab URL is shared on Reddit/Discord/Twitter, it should show a rich preview card. Implement server-side OG tag generation: (a) Add a server-side route that serves lab.html with dynamic OG meta tags based on URL params. The title should reflect the current view: 'Razzle Lab — Top WR by PPR (2024)' or 'Razzle Lab — Career QB Rankings'. (b) Description should mention key filters: 'Fantasy football screener: WR, sorted by PPR, min 8 GP, 2024 season'. (c) Use a static OG image for now (razzle-og.png — the Lab screenshot or logo). (d) Add og:url, og:type, twitter:card meta tags. (e) Server reads URL params (position, sort, season, min_gp) and generates appropriate title/description. Add a new endpoint or modify the static file serving to inject meta tags."
**Accept when**:
- Sharing a Lab URL on Discord shows a rich preview with dynamic title
- Title reflects current position/sort/season from URL params
- Description mentions active filters
- OG image shows (static image is fine)
- twitter:card meta tag present for Twitter previews
**Depends on**: none
**Size**: L
**Status**: PENDING
**Attempt**: 0/3

## Task 5: Touch-friendly filter interactions
**Requirement**: "Improve touch interactions for Lab filters on mobile: (a) Position filter chips should be larger on mobile (min 44px height, adequate spacing). (b) Team filter dropdown should open as a full-screen modal on mobile with scrollable team list and checkboxes (not a tiny dropdown). (c) Advanced stat filter add/remove buttons should be at least 44px tap targets. (d) Search input should have proper mobile keyboard behavior (search type, autocomplete off to prevent browser suggestions). (e) Season dropdown should use native select on mobile for better UX."
**Accept when**:
- Position chips are at least 44px tap height on mobile
- Team filter opens as scrollable modal on mobile
- All interactive elements meet 44px minimum tap target
- Search input behavior is mobile-appropriate
- Season dropdown uses native select on mobile
**Depends on**: Task 1
**Size**: M
**Status**: PENDING
**Attempt**: 0/3

## Task 6: Landing page mobile polish
**Requirement**: "Verify and fix the landing page (index.html) on mobile: (a) Hero section text doesn't overflow. (b) Feature cards stack single-column on mobile. (c) Agent bio cards stack single-column with readable text. (d) War Room demo section is usable on mobile. (e) Waitlist form input and button are full-width on mobile. (f) Navigation hamburger menu or scrollable nav for mobile. (g) Mascot image scales properly. (h) CTA buttons are prominent and thumb-reachable."
**Accept when**:
- Landing page loads correctly on 375px viewport
- No horizontal overflow on any section
- All cards stack single-column on mobile
- CTA buttons are prominent and full-width
- Navigation is accessible on mobile
- Waitlist form works on mobile
**Depends on**: none
**Size**: M
**Status**: PENDING
**Attempt**: 0/3

## Task 7: League Intel page mobile layout
**Requirement**: "Verify and fix the League Intel page (league-intel.html) on mobile: (a) Sleeper username input and connect button are full-width on mobile. (b) League cards stack and are readable on mobile. (c) Roster player list within expanded league cards wraps properly. (d) Disconnect button is accessible. (e) No horizontal overflow."
**Accept when**:
- League Intel page loads correctly on 375px viewport
- Connect form is full-width and usable
- League cards are readable when expanded
- Roster display works on mobile
- No horizontal overflow
**Depends on**: none
**Size**: S
**Status**: PENDING
**Attempt**: 0/3

## Task 8: Create static OG image for social previews
**Requirement**: "Create a static OG preview image (1200x630px) for social sharing: (a) Use Canvas API or a simple HTML-to-image approach to generate a branded OG image. (b) Include Razzle logo/text, 'Fantasy Football Bloomberg Terminal' tagline, razzle.lol URL. (c) Use Razzle design colors (sand background, terracotta accent, chunky border). (d) Save as frontend/assets/razzle-og.png. (e) Reference from OG meta tags in all pages."
**Accept when**:
- razzle-og.png exists at 1200x630px
- Image has Razzle branding and tagline
- All HTML pages reference the OG image
- Image looks professional when shared on social media
**Depends on**: none
**Size**: M
**Status**: PENDING
**Attempt**: 0/3

## Task 9: Deploy + mobile smoke test
**Requirement**: "Verify all Phase 30 changes work together. All JS passes syntax. All Python imports clean. All pages load correctly on mobile viewport (375px). Lab screener, charts, profile, filters all work on mobile. Landing page, League Intel responsive. OG tags present. Push to master."
**Accept when**: All JS passes syntax check. All Python files compile. All pages responsive on 375px. No horizontal overflow. OG meta tags present. Committed and pushed to master.
**Depends on**: Tasks 1-8
**Size**: M
**Status**: PENDING
**Attempt**: 0/3

---

## Loop State
```
Current Phase: 30
Current Task: 3
Current Stage: PENDING
Attempt: 0/3
Tasks Completed: 2/9
Loop Iterations: 2
```
