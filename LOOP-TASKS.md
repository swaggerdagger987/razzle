# Razzle Loop — Phase 32 Task List

> Auto-generated by Razzle Loop. Updated as tasks progress.

**Current Phase**: 32 — Formula Store (Community Marketplace)
**Exit Criterion**: Users can publish formulas from the formula builder to a public store. Store displays formula cards with name, description, position tags, creator, ratings. Visitors can browse, search, filter by position, sort by rating/popularity/newest. Formulas can be imported into user's formula set with one click. Star ratings with reviews. Backend stores formulas in SQLite. Store page accessible from Lab toolbar. Deployed to Render.

---

## Task 1: Backend formula store endpoints + database schema
**Requirement**: "Create the formula store backend: (a) New SQLite table 'formula_store': id, name, description, position_tags (JSON), stat_weights (JSON), creator_name, created_at, rating_sum, rating_count. (b) POST /api/formulas/publish — accept formula with name, description, position_tags, stat_weights, creator_name. Return published formula with id. (c) GET /api/formulas/store — list all published formulas with pagination, optional position filter, sort by (newest, rating, popularity). Return array with id, name, description, position_tags, creator_name, created_at, avg_rating, rating_count. Do NOT return stat_weights (creator IP protected — only blended scores visible). (d) POST /api/formulas/{id}/rate — accept rating (1-5) and optional review text. Store in formula_ratings table (formula_id, rating, review, created_at). Update formula_store rating_sum/rating_count. (e) GET /api/formulas/{id} — get single formula detail (for importing). Return stat_weights so user can add to their localStorage formulas."
**Accept when**:
- formula_store and formula_ratings tables exist in SQLite
- POST /api/formulas/publish creates a formula and returns id
- GET /api/formulas/store returns published formulas without weights
- POST /api/formulas/{id}/rate stores rating and updates formula
- GET /api/formulas/{id} returns full formula with weights
- All endpoints return proper JSON
**Depends on**: none
**Size**: L
**Status**: PASS
**Attempt**: 1/3
**Result**: Added formula_store table (id, name, description, position_tags JSON, stat_weights JSON, creator_name, created_at, rating_sum, rating_count) and formula_ratings table (id, formula_id, rating 1-5 CHECK, review, created_at). 5 functions in live_data.py: init_formula_store_tables(), publish_formula(), fetch_formula_store() (with position/sort/search/pagination, no weights in list), get_formula_detail() (includes weights for import), rate_formula() (updates sum+count). 4 endpoints in server.py: GET /api/formulas/store, POST /api/formulas/publish, GET /api/formulas/{id}, POST /api/formulas/{id}/rate. Tables init on startup via lifespan.

## Task 2: Formula Store browse UI
**Requirement**: "Build the Formula Store browse experience in the existing store overlay (lab.js openFormulaStore): (a) Fetch formulas from GET /api/formulas/store on open. (b) Display formula cards in a 2-column grid: formula name, description (truncated), position badges, creator name, average rating (stars), rating count. (c) Position filter tabs (ALL/QB/RB/WR/TE). (d) Sort dropdown: Newest, Highest Rated, Most Popular. (e) Search input for formula name. (f) 'Import' button on each card that adds the formula to localStorage. (g) Show imported status if formula already in user's set. (h) Razzle design: chunky borders, position-colored badges, star rating display."
**Accept when**:
- Store overlay shows published formulas on open
- Position filter tabs filter correctly
- Sort dropdown works (newest, rating, popular)
- Search filters by name
- Import button adds formula to user's localStorage
- Already-imported formulas show 'Imported' badge
- Design follows Razzle comic-strip aesthetic
**Depends on**: Task 1
**Size**: L
**Status**: PENDING
**Attempt**: 0/3

## Task 3: Publish flow from formula builder
**Requirement**: "Add publish-to-store flow in the formula builder: (a) After saving a formula in the builder, show a 'Publish to Store' button. (b) Clicking opens a publish modal: formula name (pre-filled), description textarea, position tags (checkbox: QB/RB/WR/TE), creator name input. (c) Submit calls POST /api/formulas/publish. (d) Show success message with link to store. (e) The publish overlay already exists in lab.html (publishOverlay) — wire it up. (f) Prevent duplicate publishing of same formula name by same creator."
**Accept when**:
- Publish button appears after saving a formula
- Publish modal has description + position tags + creator name
- Submit creates formula in store
- Success message shows
- Duplicate prevention works
**Depends on**: Task 1
**Size**: M
**Status**: PENDING
**Attempt**: 0/3

## Task 4: Rating and review system
**Requirement**: "Add rating UI to formula store cards: (a) Click rating area opens a rate modal: 1-5 star selector + optional review textarea. (b) Submit calls POST /api/formulas/{id}/rate. (c) After rating, update the card's displayed rating. (d) Show user's own rating if they've already rated (stored in localStorage). (e) Star display: filled stars for rating, empty for remainder. Orange color."
**Accept when**:
- Clicking rating area opens rate modal
- Star selector works (1-5)
- Review textarea is optional
- Rating submits and updates display
- localStorage tracks user's ratings
**Depends on**: Tasks 1, 2
**Size**: M
**Status**: PENDING
**Attempt**: 0/3

## Task 5: Formula detail view + import
**Requirement**: "When importing a formula from the store: (a) Fetch GET /api/formulas/{id} to get full stat_weights. (b) Add formula to localStorage formulas with a 'store_id' marker so it can be identified as imported. (c) Formula immediately appears as a column in the Lab. (d) Show toast notification 'Formula imported!' (e) If formula already imported, show 'Already imported' state."
**Accept when**:
- Import button fetches formula weights from API
- Formula added to localStorage and appears as column
- Toast notification shows on import
- Already-imported state prevents duplicates
**Depends on**: Tasks 1, 2
**Size**: M
**Status**: PENDING
**Attempt**: 0/3

## Task 6: Seed store with starter formulas
**Requirement**: "Pre-populate the formula store with 5-8 useful starter formulas so it doesn't look empty on launch: (a) Dynasty Composite: PPR/G * 0.5 + rushing_yards * 0.2 + receiving_yards * 0.2 + touchdowns * 0.1. (b) Efficiency Score: yards_per_carry * 0.3 + catch_rate * 0.3 + target_share * 0.4. (c) Workload Index: carries * 0.3 + targets * 0.4 + snap_share * 0.3. (d) Breakout Potential: age-adjusted PPR with youth premium. (e) QB Value: passing_yards * 0.3 + passing_tds * 0.3 + rushing_yards * 0.2 + CPOE * 0.2. Add these via a seed script or inline in server.py bootstrap."
**Accept when**:
- Store shows 5+ pre-populated formulas on first load
- Each formula has a name, description, and position tags
- Formulas are useful and produce reasonable scores
**Depends on**: Task 1
**Size**: S
**Status**: PENDING
**Attempt**: 0/3

## Task 7: Deploy + smoke test
**Requirement**: "Verify all Phase 32 changes work together. All JS passes syntax. All Python imports clean. Store page renders with seed formulas. Publish flow works. Rating works. Import works. Push to master."
**Accept when**: All JS passes syntax check. All Python files compile. Store has seed formulas. Full publish→browse→rate→import flow works. Committed and pushed to master.
**Depends on**: Tasks 1-6
**Size**: M
**Status**: PENDING
**Attempt**: 0/3

---

## Loop State
```
Current Phase: 32
Current Task: 1
Current Stage: PENDING
Attempt: 0/3
Tasks Completed: 0/7
Loop Iterations: 0
```
