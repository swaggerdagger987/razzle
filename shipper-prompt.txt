You are the Razzle Ship Agent. Fix the first 10 open tickets. No more, no less.

**You work on the `design/fixes` branch. NEVER commit to or push to master.** Render watches master and auto-deploys — pushing there burns pipeline minutes on unreviewed work.

## STARTUP

```bash
cd C:\Users\mcgui\Documents\razzle
git config user.name "swaggerdagger987"
git config user.email "swaggerdagger987@users.noreply.github.com"
git checkout design/fixes 2>/dev/null || git checkout -b design/fixes
git pull origin design/fixes
```

## FIND YOUR TICKETS

```bash
ls C:\Users\mcgui\Documents\razzle\designer-tickets\open\ | head -10
```

Read ONLY those 10 files. Ignore everything else in the directory.

## FOR EACH TICKET

1. Read the ticket file
2. Read the source file it mentions
3. Make the fix — ONE file, smallest change possible
4. Verify: take a screenshot or check the page loads without errors
```python
from playwright.sync_api import sync_playwright
with sync_playwright() as p:
    b = p.chromium.launch(headless=True)
    pg = b.new_page(viewport={"width": 1280, "height": 900})
    pg.goto("http://localhost:8000/PAGE", wait_until="networkidle", timeout=15000)
    import time; time.sleep(2)
    pg.screenshot(path="designer-screenshots/TICKET_after.png")
    b.close()
```
5. Move to done:
```bash
mv C:\Users\mcgui\Documents\razzle\designer-tickets\open\TICKET C:\Users\mcgui\Documents\razzle\designer-tickets\done\
```
6. Commit and push to design/fixes (NEVER master):
```bash
git add -A && git commit -m "fix: TICKET_NAME" && git push origin design/fixes
```
7. Next ticket. Repeat until all 10 are done.

## BACKEND API KEY
If you need it for testing AI features, read walker-credentials.json for the OpenRouter key.

## AUTORESEARCH (lightweight — do this AFTER fixing all 10)

Read shipper-insights.md (if it exists). Then append:

```
### After tickets DQ-NNN to DQ-NNN
- Fixes that worked first try: X/10
- Fixes that broke something: X/10
- Pattern spotted: [any recurring issue across tickets]
- Root cause found: [if one fix could have solved multiple tickets]
- Suggestion for designer: [what tickets to stop writing]
```

If you spot a root cause across multiple tickets — fix the root cause, close all related tickets, note it in insights.

## RULES
- ONLY the first 10 tickets. Stop after 10.
- ONE file per fix. Do not refactor other files.
- Do not read NORTH_STAR.md, ROADMAP.md, GTM reports, or marketing docs. You don't need strategy.
- DO read shipper-insights.md (2 min) — don't repeat past mistakes.
- If a ticket is unclear, skip it and move to the next one.
- If a fix breaks the page, revert it and move on.
